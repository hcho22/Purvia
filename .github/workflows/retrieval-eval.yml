# US-035: PR retrieval-eval workflow. Runs the 60-question golden set
# (US-033) against PR head AND main, then posts a delta-vs-main markdown
# comment on the PR. The delta comment is advisory — it never fails the build,
# regardless of delta sign, and updates an existing bot comment in place across
# pushes.
#
# US-009: the PR run additionally executes the E6 second-workspace zero-leak
# eval (--include-e6), which IS a hard gate: a DETECTED cross-workspace leak (or
# a structurally blind positive control) fails the eval step and blocks the
# merge. E6 is a pinned security invariant (CONTEXT E8), so a real leak VERDICT
# is allowed to block per-PR — unlike the LLM-judged quality metrics, which only
# ever comment here. A DETERMINISTIC E6 failure (e.g. an empty/mis-seeded corpus
# or a deterministic 4xx) also BLOCKS: it is reproducible, so it can't flake and
# keeps failing until fixed — but the runner writes the E4 results + summary
# FIRST, then exits 1, so E4 output is never discarded. A transient EXECUTION
# failure (E6 makes ~300 live OpenAI + DB calls) is a different thing: it is
# retried with backoff, then surfaced loudly but NON-BLOCKING, so an infra blip
# can't fail-block an innocent PR.
#
# US-059: the PR run ALSO executes the E7 escalation tripwire — the DETERMINISTIC
# deflection-pipeline legs only (P1a retrieval-gate decisions + the P1b no-access
# replay + the US-058 P1b non-disclosure byte-equality assertion), reusing this
# job's already-seeded corpus + Supabase stack. Like E6 it is a HARD gate: a
# P1a/P1b row that clears the retrieval gate (a retrieval-leg false-resolve / a
# gold leak to a no-access viewer) or a non-disclosure byte mismatch exits the
# runner non-zero and blocks the merge. It is pure arithmetic on cosine scores —
# NO LLM judge — so a real verdict can't flake. The LLM-judged P2/P3 deflection
# sweep is deliberately NOT here (it lives in escalation-eval-weekly.yml); a judge
# wobble must never red-bar an innocent merge.
#
# US-105: determinism, NOT buyer preference, decides what may block a merge.
# The whole four-workflow split reduces to ONE rule (formalized in code as
# evals/gate/placement.py::placement_for and in docs/evals.md "Determinism decides
# merge-blocking"):
#
#   * DETERMINISTIC gates - recall@k / MRR / nDCG, the pinned E4/E6/AU4/E7-P1b
#     invariants, and the deterministic retrieval-gate tripwire (pure arithmetic /
#     binary asserts) - run PER-PR here and a `fail` BLOCKS THE MERGE (non-zero
#     exit on this pull_request workflow).
#   * NON-DETERMINISTIC gates - the four RAGAS scores, the runtime faithfulness
#     gate, the full E7 P2/P3 deflection + false-resolve sweep (all LLM-judged) -
#     are STRUCTURALLY scheduled-only (retrieval-eval-ragas-weekly.yml /
#     retrieval-eval-nightly.yml / permissions-scale-eval.yml + escalation-eval-
#     weekly.yml): a `fail` there fails the scheduled workflow + files one issue
#     per tag, NEVER a merge block. A judge wobble must never red-bar an innocent
#     merge. The gate-declaration loader REJECTS a config that requests a per-PR
#     `fail` on a non-deterministic gate (evals/gate/declaration.py, a structural
#     load error) so the split cannot be reconfigured away.
#
# This workflow is the per-PR / deterministic / may-block face of that rule. It
# runs the deterministic security invariants (E4 + E6 in the `retrieval-eval` job;
# AU4 in the `au4-security-invariants` job below) and the deterministic E7 P1a/P1b
# retrieval-leg tripwire - so a cross-workspace leak introduced by a PR fails
# BEFORE merge. AU4 imports backend/main.py (docling/torch), which the slim
# requirements-ci.txt deliberately avoids, so it runs as a SEPARATE job (its own
# runner = its own disk budget) rather than bloating the dual-checkout eval job.
#
# US-105 (review follow-up): the two jobs trigger on DIFFERENT diffs, split by a
# lightweight `changes` job (dorny/paths-filter). The `retrieval-eval` job keeps
# its narrower RETRIEVAL-only scope, so it does NOT pay the dual-checkout + double
# corpus-seed cost on a backend/main.py-only PR. The `au4-security-invariants` job
# ADDITIONALLY covers the API-edge endpoint surface (backend/main.py): AU4's Phase 1
# auth floor (`get_user`) and its Phase 3 widget transcript / first-message opaque-
# token binding checks are ENDPOINT-level logic in main.py, not the RLS migrations,
# so a PR weakening the transcript binding, the first-message key gate, or the auth
# floor directly in main.py re-runs AU4 before merge (closing the coverage gap where
# a main.py-only PR would previously trigger NO per-PR security gate at all). This
# only refines WHICH deterministic per-PR gate runs for a given diff; determinism-
# decides-merge-blocking (US-105) is unchanged - a `fail` in either job still blocks.
#
# Reranker modes (cohere / voyage / llm) are NOT exercised here — they
# cost real money per call and add API-dependency flake to PR checks.
# The nightly workflow (retrieval-eval-nightly.yml) is where rerankers
# eventually land once the runner gains reranker support.
#
# US-042: the runner now walks 60 questions × 3 modes × 3 viewers ×
# 2 filter strategies, so the wall-clock budget grows ~3× (each viewer
# adds an extra match_chunks round-trip per question per mode; post-
# filter re-uses the owner ranking). Both PR and main are run, so the
# combined budget is ~6×. timeout-minutes bumped accordingly. Note that
# `main` predates US-042 — its runner doesn't accept `--viewers`, so it
# runs the legacy single-viewer flow; the PR's runner produces the
# richer JSON, and `by_mode` (the canonical full_access × pre_filter
# numbers) is preserved on both sides for diff_results to consume.

name: Retrieval eval (PR)

on:
  pull_request:
    # Trigger on EVERY PR to the default branch, with NO workflow-level `paths:`
    # filter. This workflow's deterministic security gates (E6 in the
    # `retrieval-eval` job -> check "PR vs main"; AU4 in `au4-security-invariants`;
    # the offline harness guards) are intended to become REQUIRED status checks in
    # branch protection. GitHub blocks a PR forever if a required check is never
    # reported, so the workflow must RUN on every PR - even a docs/frontend/CI-only
    # diff that touches none of the eval paths - or those PRs would hang with the
    # required checks pending.
    #
    # Per-job path routing is NOT lost by dropping `paths:` here: the `changes` job
    # (dorny/paths-filter, below) is the single source of per-job path truth. On an
    # unrelated diff every per-job filter reports false, so each heavy job is gated
    # `false` by its `if: needs.changes.outputs.<x> == 'true'` and REPORTS "skipped"
    # - which branch protection treats as a PASS. On an eval/backend-touching PR the
    # relevant job(s) run and can hard-block on a real failure, unchanged. A bare
    # (path-less) trigger + internal `if:` routing is the GitHub-recommended shape
    # for a required check that must always report while only doing real work on a
    # relevant diff.
    branches:
      - main

permissions:
  contents: read
  pull-requests: write

concurrency:
  group: retrieval-eval-${{ github.event.pull_request.number }}
  cancel-in-progress: true

jobs:
  # US-105 (review follow-up): compute a per-job path split from the PR diff so the
  # two deterministic gates trigger independently. The trigger is now path-less
  # (`pull_request` on `branches: [main]`) and runs on EVERY PR to main, so this job
  # is the sole per-job path arbiter - it decides WHICH of the two jobs runs. The
  # retrieval-eval job stays retrieval-only (it must not pay
  # the dual-checkout + double corpus-seed cost on a main.py-only PR), while the AU4
  # job additionally covers the API-edge endpoint surface (backend/main.py), whose
  # Phase 1 auth floor + Phase 3 widget opaque-token binding checks are endpoint-
  # level, not RLS. A skipped downstream job (its filter false) reports "skipped",
  # which branch protection treats as a pass, so a retrieval-only PR does not hang on
  # the AU4 check and vice versa.
  changes:
    name: Detect changed paths
    runs-on: ubuntu-latest
    outputs:
      retrieval: ${{ steps.filter.outputs.retrieval }}
      au4: ${{ steps.filter.outputs.au4 }}
      guards: ${{ steps.filter.outputs.guards }}
    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4

      - name: Filter changed paths
        id: filter
        uses: dorny/paths-filter@v3
        with:
          filters: |
            retrieval:
              - 'backend/retrieval.py'
              - 'backend/chunking.py'
              - 'backend/embeddings.py'
              - 'backend/escalation.py'
              - 'backend/**/*prompt*'
              - 'evals/retrieval/**'
              - 'db_seed/corpus/**'
              - 'db_seed/corpus_seed.py'
              - 'supabase/migrations/**'
              - '.github/workflows/retrieval-eval.yml'
            au4:
              - 'backend/main.py'
              - 'backend/permissions.py'
              - 'supabase/migrations/**'
              - 'backend/test_au4_auth_attacks.py'
              # The RLS-hardening regression test (PR #126) rides the same job and
              # the same booted Supabase stack as AU4, so a change to the test
              # itself must re-run the job. supabase/migrations/** (already above)
              # is the load-bearing trigger: a future migration that re-opens the
              # F1/F2/F3 holes this test guards fails BEFORE merge.
              - 'backend/test_sec_rls_hardening.py'
              - '.github/workflows/retrieval-eval.yml'
            guards:
              - 'evals/retrieval/**'
              - 'evals/permissions_scale/**'
              # The answer-gate rubric guard reads BOTH rubrics, so a change to
              # either implementation must trigger it - the whole point is to
              # catch the two drifting apart (AGENTS.md invariant 8). The
              # eval-side rubric lives under evals/retrieval/ above.
              - 'backend/escalation.py'
              - 'backend/test_answer_gate_rubric.py'
              # The two runtime-gate modules are guards in this job too: they pin
              # the JUDGE_TEMPERATURE resolution matrix and that both gates
              # actually send the pin. A PR touching only one of them is the edit
              # most likely to weaken it, so it must re-run the job.
              - 'backend/test_answer_gate.py'
              - 'backend/test_faithfulness_gate.py'
              # The same guard also reads backend/main.py's SOURCE, to pin that
              # the judge boot warning is still called there and still wired to
              # the SUPABASE_SERVICE_ROLE_KEY widget-surface predicate (AGENTS.md
              # invariant 10). The edit that breaks that scoping is a main.py-only
              # edit, which without this entry would run no guard at all.
              - 'backend/main.py'
              - '.github/workflows/retrieval-eval.yml'

  # Invariant 12 ("measured nothing must never be reportable as a measurement")
  # is pinned by two offline test modules - one per half: derived gold
  # (evals/permissions_scale/test_degenerate_guard.py) and authored gold
  # (evals/retrieval/test_empty_gold_guard.py). Until now neither was executed by
  # any workflow, so the guard against a silently-green harness was itself
  # enforced only by a command in a markdown file.
  #
  # The answer-gate rubric guard (backend/test_answer_gate_rubric.py, issue #104)
  # joins them for the same reason. It is the ONLY layer below the weekly E7 sweep
  # that can catch the runtime rubric and its offline mirror drifting apart, and
  # drift makes the weekly false-resolve number a measurement of something the
  # buyer does not ship (AGENTS.md invariant 8). A drift guard that runs only when
  # someone remembers to run it provides none of that protection - which is how
  # the issue-#104 gap survived from PR #99 to the 2026-08-03 sweep.
  #
  # backend/test_answer_gate.py and backend/test_faithfulness_gate.py join them
  # for a narrower but larger-stakes reason: they are the ONLY per-PR enforcement
  # of the issue-#104 sampler pin. Between them they pin the JUDGE_TEMPERATURE
  # resolution matrix (unset / blank / `none` / out-of-range) and that both
  # runtime gates actually SEND that pin on the wire, plus the boot warning's
  # scoping and the fail-closed one-call-no-retry paths on every judge failure
  # shape. Without them an edit dropping `**_judge_sampling_kwargs()` from
  # `_judge_parse`, or flipping DEFAULT_JUDGE_TEMPERATURE, leaves the whole suite
  # green - un-pinning the send/escalate verdict (AGENTS.md invariant 8) with no
  # signal at all. That is the same "runs only when someone remembers to run it"
  # failure the rubric guard above exists to close, applied to the property the
  # rubric guard is scoped around.
  #
  # The US-120 generation guard (evals/retrieval/test_us120_generation_guard.py)
  # is the third invariant-12 half: it pins that the gpt-5.6-luna answer generator
  # refuses a truncated/empty answer (a reasoning-budget cutoff) instead of
  # returning "" for the judge to score as a real low result.
  #
  # The judge-SDK-compat guard (evals/retrieval/test_judge_sdk_compat.py) is a
  # dependency-drift tripwire: the anthropic 1.0 major REMOVED the `temperature`
  # kwarg from messages.create(), so a loose `anthropic>=...` in
  # evals/retrieval/requirements.txt let CI resolve 1.2.0 and crash BOTH weeklies
  # at the deterministic offline judge (issues #124/#125). It asserts the pin still
  # excludes the temperature-less 1.x major - offline, no anthropic import needed,
  # so it runs in this slim job and fails the PR that would re-open the crash.
  #
  # The E7 answer-gate parity guard pins the additive weekly check itself: it
  # replays exact P2/P3 question/draft/context inputs, refuses runtime failures as
  # UNMEASURED, and surfaces both verdicts on a mismatch.
  #
  # All eight are offline by construction - no DB, no secrets, no network, inputs
  # built in-process (the two gate modules drive fake judge clients, so there is
  # no network call to make) - which is what lets this job gate a PR without the
  # Supabase stack the two jobs below pay for. The rubric module's LIVE half
  # skips cleanly when no API keys are present, and it reads the eval-side rubric
  # via `ast` rather than importing evals.retrieval.runner, so only its offline
  # half runs here. It is deliberately its own job so it still reports on a
  # permissions-scale-only change, where `retrieval-eval` is skipped.
  eval-harness-guards:
    name: Eval-harness guards (offline, deterministic, may block)
    needs: changes
    if: needs.changes.outputs.guards == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: evals/retrieval/requirements-ci.txt

      - name: Install Python deps
        run: |
          # Same slim install as the eval job: all eight guard modules import only
          # what requirements-ci.txt already covers (asyncpg / httpx / openai /
          # pyyaml / pyjwt), never the ML stack. The rubric guard reaches
          # backend/escalation.py -> retrieval -> embeddings, which stays inside
          # that set (openai brings pydantic); the two gate modules import the
          # same chain plus `openai.AsyncOpenAI` for the fake judge client they
          # cast to, and never `anthropic`. Verified against a clean venv.
          python -m pip install --upgrade pip
          pip install -r evals/retrieval/requirements-ci.txt

      - name: Run the offline harness guards
        # No `|| true`, no skip-on-unset-env: these eight modules must always run
        # and a non-zero exit must block the merge. A guard that can go quiet is
        # the failure mode it exists to catch.
        run: |
          python -m evals.retrieval.test_empty_gold_guard
          python -m evals.retrieval.test_us120_generation_guard
          python -m evals.retrieval.test_judge_sdk_compat
          python -m evals.retrieval.test_e7_answer_gate_parity
          python -m evals.permissions_scale.test_degenerate_guard
          python -m backend.test_answer_gate_rubric
          python -m backend.test_answer_gate
          python -m backend.test_faithfulness_gate

  retrieval-eval:
    name: PR vs main
    needs: changes
    if: needs.changes.outputs.retrieval == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4
        with:
          path: pr

      - name: Checkout main into side directory
        uses: actions/checkout@v4
        with:
          ref: main
          path: main

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: pr/evals/retrieval/requirements-ci.txt

      - name: Install Supabase CLI
        # Pinned, NOT `latest`: a newer Supabase CLI changed the local stack's
        # default auth key format / JWT signing, so the runner's self-minted
        # HS256 JWTs were gateway-rejected (403) on the first match_chunks call
        # — even after sourcing JWT_SECRET/ANON_KEY from `supabase status`
        # (below), because the break is the key *format*, not just its value.
        # 2.90.0 matches local dev, where the eval's auth path is verified
        # working. Bump only alongside a runner update that handles the newer
        # Supabase key formats. Keep the other eval workflows on the same pin.
        uses: supabase/setup-cli@v1
        with:
          version: 2.90.0

      - name: Start local Supabase stack
        working-directory: pr
        run: |
          # `supabase start` brings up pgvector + PostgREST + auth + applies
          # all migrations in supabase/migrations/. The PR's migration set
          # is used because we want to eval against the schema the PR ships.
          supabase start
          # Surface the running stack's coordinates to subsequent steps.
          # `-o env` emits `KEY="value"` pairs; strip quotes for $GITHUB_ENV.
          #
          # ANON_KEY + JWT_SECRET are surfaced (alongside API_URL /
          # SERVICE_ROLE_KEY / DB_URL) so the eval authenticates against THIS
          # instance's actual credentials rather than the runner's hardcoded
          # local-dev fallbacks. The runner mints the per-viewer authenticated
          # JWTs with SUPABASE_JWT_SECRET (falling back to a baked-in local
          # secret) and sends SUPABASE_ANON_KEY as the PostgREST `apikey` for
          # those user-JWT requests (falling back to the privileged
          # service-role key). Those fallbacks only line up when the running
          # `supabase start` default secret/keys match the baked-in constants;
          # an UNPINNED CLI can ship a different default JWT secret or key
          # format, after which the very first authenticated match_chunks call
          # is gateway-rejected (403) and the eval crashes. The CLI is now
          # pinned (above) to dodge that drift; sourcing from `supabase status`
          # stays as defense so a future pin bump can't silently desync auth.
          supabase status -o env > /tmp/supabase.env
          grep -E '^(API_URL|ANON_KEY|SERVICE_ROLE_KEY|JWT_SECRET|DB_URL)=' /tmp/supabase.env \
            | sed -E 's/^(.*)="(.*)"$/\1=\2/' \
            >> "$GITHUB_ENV"

      - name: Install Python deps (from PR)
        working-directory: pr
        run: |
          # Slim install: backend/requirements.txt drags in torch +
          # transformers + docling and exceeds the runner's disk budget.
          # requirements-ci.txt lists only what runner.py / corpus_seed.py /
          # diff_results.py import on the retrieval-only path (no
          # --include-generation in CI). See requirements-ci.txt comments.
          python -m pip install --upgrade pip
          pip install -r evals/retrieval/requirements-ci.txt

      - name: Seed corpus + run eval (PR)
        working-directory: pr
        env:
          SUPABASE_URL: ${{ env.API_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ env.SERVICE_ROLE_KEY }}
          # The eval mints authenticated viewer JWTs and calls match_chunks
          # under them; SUPABASE_JWT_SECRET signs those JWTs and
          # SUPABASE_ANON_KEY is the PostgREST apikey for the user-JWT
          # requests. Both come from the running stack (above) so the eval does
          # not fall back to baked-in local-dev defaults that a newer Supabase
          # may no longer match (the 403-on-first-query failure mode).
          SUPABASE_ANON_KEY: ${{ env.ANON_KEY }}
          SUPABASE_JWT_SECRET: ${{ env.JWT_SECRET }}
          CORPUS_SEED_DATABASE_URL: ${{ env.DB_URL }}
          DATABASE_URL: ${{ env.DB_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          python -m db_seed.corpus_seed
          # US-042: --viewers all is the default; spelled out here so the
          # workflow log clearly records the run shape. Drops to --viewers
          # full if the new tables are ever too noisy in PR comments.
          #
          # US-009: --include-e6 runs the second-workspace zero-leak eval. It is
          # a PINNED SECURITY INVARIANT (CONTEXT E8 "Security/correctness gate"):
          # a DETECTED cross-workspace leak — or a structurally blind positive
          # control — makes the runner exit non-zero, which FAILS this step and
          # blocks the merge. This leak VERDICT is deliberate and non-downgradable
          # (a fail-the-build behavior in this otherwise comment-only workflow);
          # the delta-vs-main comment below stays advisory. Leak DETECTION is
          # deterministic, so a real verdict is trustworthy — but eval EXECUTION
          # is not: E6 makes ~300 live OpenAI + asyncpg calls, so a transient blip
          # (rate-limit / network / DB) is retried with backoff and then surfaced
          # as a loud but NON-BLOCKING execution error (recorded in the result
          # JSON + summary.md, no exit 1). Thus an infra flake cannot fail-block
          # an innocent PR, mirroring the graceful degradation in
          # backend/test_au4_auth_attacks.py. A DETERMINISTIC E6 failure (e.g. an
          # empty/mis-seeded corpus or a deterministic 4xx) is neither a leak nor
          # a flake: it ALSO exits 1 and blocks — but ONLY after the E4 results
          # JSON + summary.md are written, so E4 output is never discarded.
          python -m evals.retrieval.runner \
            --viewers all --include-e6 --out /tmp/pr.json

      - name: E7 escalation tripwire (PR, deterministic — US-059)
        working-directory: pr
        env:
          SUPABASE_URL: ${{ env.API_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ env.SERVICE_ROLE_KEY }}
          SUPABASE_ANON_KEY: ${{ env.ANON_KEY }}
          SUPABASE_JWT_SECRET: ${{ env.JWT_SECRET }}
          CORPUS_SEED_DATABASE_URL: ${{ env.DB_URL }}
          DATABASE_URL: ${{ env.DB_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          # US-059 per-PR tripwire: the DETERMINISTIC escalation legs ONLY — the
          # P1a retrieval-gate decisions (pure arithmetic on the pre-fusion cosine,
          # no LLM), the P1b no-access replay, and the US-058 P1b non-disclosure
          # byte-equality assertion. All three are PINNED `fail`: a P1a/P1b row that
          # clears the retrieval gate (a retrieval-leg false-resolve / a gold leak
          # to a no-access viewer) or a P1b customer output that differs from the
          # generic deferral exits the runner non-zero, FAILS this step, and BLOCKS
          # the merge — exactly like the E6 zero-leak gate above. Leak detection is
          # deterministic, so a real verdict is trustworthy and non-downgradable;
          # the runner writes /tmp/e7-pr.json FIRST, then exits 1, so the detail is
          # never discarded. The consolidated false-resolve ceiling gate (US-059)
          # also runs here but is structurally INERT per-PR — its gated rate is the
          # faithfulness leg (P3), which is not scored here, so it is unmeasured and
          # cannot breach; the retrieval-leg P1a/P1b false-resolves are pinned above.
          #
          # NO --include-p2/--include-p3/--sweep: those draft + call the OFFLINE
          # Claude faithfulness judge, so they are LLM-judged and live in the weekly
          # escalation workflow (escalation-eval-weekly.yml). A judge wobble must
          # never red-bar an innocent merge; the faithfulness-leg false-resolve
          # carries an accepted up-to-a-week detection latency, mitigated by this
          # deterministic retrieval-leg tripwire (docs/evals.md "E7 CI placement").
          # No ANTHROPIC_API_KEY is needed or passed — this runs on the slim
          # requirements-ci.txt env installed above (e7_runner's deterministic path
          # imports only escalation/retrieval, both already on that env).
          #
          # Reuses THIS job's already-seeded corpus + running Supabase stack; the
          # P1b leg rewrites chunk_acl per question via asyncpg (DATABASE_URL), the
          # same E4 reset_viewer_acls machinery the PR eval above already exercised.
          # The main reseed below restores corpus state for the main runner.
          python -m evals.retrieval.e7_runner --include-p1b --out /tmp/e7-pr.json

      - name: Reseed corpus + run eval (main)
        working-directory: main
        env:
          SUPABASE_URL: ${{ env.API_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ env.SERVICE_ROLE_KEY }}
          # Same as the PR step: authenticate against the running stack's real
          # secret/anon key rather than baked-in local-dev fallbacks.
          SUPABASE_ANON_KEY: ${{ env.ANON_KEY }}
          SUPABASE_JWT_SECRET: ${{ env.JWT_SECRET }}
          CORPUS_SEED_DATABASE_URL: ${{ env.DB_URL }}
          DATABASE_URL: ${{ env.DB_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          # The main checkout reuses the PR's Python env. If main predates
          # US-032 (no corpus_seed yet), fall back to PR's already-seeded
          # corpus so the main eval still runs against a populated DB —
          # the comparison is then "main code paths vs PR data" which is
          # the meaningful question for retrieval-touching PRs anyway.
          if [ -f db_seed/corpus_seed.py ]; then
            python -m db_seed.corpus_seed
          else
            echo "main lacks db_seed/corpus_seed.py — using PR's seed in place"
          fi
          python -m evals.retrieval.runner --out /tmp/main.json

      - name: Render delta-vs-main markdown comment
        working-directory: pr
        run: |
          python -m evals.retrieval.ci.diff_results \
            /tmp/main.json /tmp/pr.json > /tmp/comment.md
          echo "--- comment preview ---"
          cat /tmp/comment.md

      - name: Post / update PR comment
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('/tmp/comment.md', 'utf8');
            const marker = '<!-- retrieval-eval-bot-comment -->';
            const { owner, repo } = context.repo;
            const issue_number = context.issue.number;
            const { data: existing } = await github.rest.issues.listComments({
              owner, repo, issue_number, per_page: 100,
            });
            const prior = existing.find(c => c.body && c.body.includes(marker));
            if (prior) {
              await github.rest.issues.updateComment({
                owner, repo, comment_id: prior.id, body,
              });
              core.info(`updated existing comment ${prior.id}`);
            } else {
              const { data: created } = await github.rest.issues.createComment({
                owner, repo, issue_number, body,
              });
              core.info(`created new comment ${created.id}`);
            }

  # US-105: the API-layer auth-attack suite (AU4) is a DETERMINISTIC per-PR
  # security invariant (`au4_auth_attacks` in evals/gate/classes.py, pinned `fail`),
  # the API-edge counterpart to the E6 cross-workspace boundary the retrieval-eval
  # job already gates. It exits NON-ZERO on any breach (a cross-workspace read, a
  # broken opaque-token binding, a customer token that retrieves), so a leak
  # introduced by a PR fails BEFORE merge. Its assertions are exact `== 0` /
  # `rejected` binary checks - no LLM judge - so a real verdict can never flake and
  # is allowed to hard-block, exactly like E4/E6/E7-P1b.
  #
  # It is a SEPARATE job (not a step in `retrieval-eval`) because it imports
  # backend/main.py, which pulls docling/torch - the multi-GB ML stack the slim
  # requirements-ci.txt deliberately omits to fit the eval job's dual pr+main
  # checkout + double corpus seed. A dedicated job gets its own fresh runner (own
  # ~14 GB disk), so the heavy install here never competes with the eval job.
  # AU4 self-seeds its W1/W2/W3 fixtures (no corpus_seed) and skips cleanly
  # (exit 0) when the required env is unset, so it is never an infra-flake block.
  au4-security-invariants:
    name: AU4 API-layer auth attacks (deterministic, may block)
    needs: changes
    if: needs.changes.outputs.au4 == 'true'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout PR head
        uses: actions/checkout@v4

      - name: Set up Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: pip
          cache-dependency-path: backend/requirements.txt

      - name: Install Supabase CLI
        # Pinned 2.90.0, same rationale as the retrieval-eval job above: a newer
        # CLI changed the local stack's JWT key format, gateway-rejecting (403)
        # the suite's self-minted HS256 JWTs. Keep the pin in lockstep across
        # every eval workflow.
        uses: supabase/setup-cli@v1
        with:
          version: 2.90.0

      - name: Start local Supabase stack
        run: |
          # Brings up pgvector + PostgREST + auth and applies the PR's migrations
          # (supabase/migrations/) - the RLS boundary AU4 attacks. Surface the
          # running stack's real coordinates so the suite authenticates against
          # THIS instance rather than baked-in local-dev fallbacks (the 403-on-
          # first-query drift the CLI pin also guards).
          supabase start
          supabase status -o env > /tmp/supabase.env
          grep -E '^(API_URL|ANON_KEY|SERVICE_ROLE_KEY|JWT_SECRET|DB_URL)=' /tmp/supabase.env \
            | sed -E 's/^(.*)="(.*)"$/\1=\2/' \
            >> "$GITHUB_ENV"

      - name: Install backend deps
        run: |
          # Full backend install: AU4 imports backend/main.py (fastapi + docling +
          # torch + …). This is the heavy stack requirements-ci.txt avoids; it is
          # acceptable HERE because this job's runner is dedicated to AU4 alone.
          python -m pip install --upgrade pip
          pip install -r backend/requirements.txt

      - name: Run AU4 API-layer auth attacks (deterministic - may block)
        env:
          SUPABASE_URL: ${{ env.API_URL }}
          SUPABASE_ANON_KEY: ${{ env.ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ env.SERVICE_ROLE_KEY }}
          SUPABASE_JWT_SECRET: ${{ env.JWT_SECRET }}
          DATABASE_URL: ${{ env.DB_URL }}
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          # Exact `== 0` / `rejected` binary security assertions across the auth
          # floor, the cross-workspace / no-membership data boundary, and (when the
          # Epic-E tables are present) the widget/conversation surface (US-093).
          # Any breach raises and exits non-zero, FAILING this required check and
          # BLOCKING the merge - the deterministic, pinned-`fail` posture (US-102/
          # US-105). Missing env → clean skip (exit 0), never an infra-flake block.
          python -m backend.test_au4_auth_attacks

      - name: Run RLS-hardening regression (deterministic - may block)
        env:
          SUPABASE_URL: ${{ env.API_URL }}
          SUPABASE_ANON_KEY: ${{ env.ANON_KEY }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ env.SERVICE_ROLE_KEY }}
          SUPABASE_JWT_SECRET: ${{ env.JWT_SECRET }}
          DATABASE_URL: ${{ env.DB_URL }}
        run: |
          # PR #126 RLS audit regression: asserts the F1/F2/F3 tightenings END TO
          # END against live PostgREST + RLS (cross-tenant profiles email
          # enumeration; unconstrained documents.workspace_id writes; the
          # cross-user chunk_acl boolean oracle). Reuses THIS job's already-booted
          # Supabase stack (same env block as AU4; no OpenAI needed, so omitted).
          # Any breach exits non-zero, FAILS this required check, and BLOCKS the
          # merge - the same deterministic pinned-`fail` posture as AU4.
          #
          # Anti-silent-skip preflight: the test clean-skips (exit 0) when
          # DATABASE_URL is unset. In THIS job the env IS set, so it runs against
          # live RLS - but the gate must never pass green while testing nothing. If
          # a future edit drops DATABASE_URL from this job, fail LOUDLY here rather
          # than let the security gate go silently green (the repo's loud-failure
          # precedent, e.g. the eval-publish empty-token preflight).
          if [ -z "${DATABASE_URL:-}" ]; then
            echo "::error::DATABASE_URL is empty - the RLS-hardening security gate would clean-skip and pass while testing nothing. Failing loudly instead. Restore the booted Supabase stack's DB_URL to this step's env."
            exit 1
          fi
          python -m backend.test_sec_rls_hardening
