# RED_TEAM

Four fresh adversaries (who did not see the build reasoning) attacked the finished package on 2026-07-16.
Their objections appear VERBATIM below. Each carries a disposition: **fixed** (with the fix linked), **accepted** (a known limit, said publicly in the deliverable named), or **rebutted** (with the argument).
An objection quietly deleted is a failed build; nothing was deleted.

Tally: 40 objections (3 kill, 26 major, 11 minor).
Dispositions: 31 fixed in-package (several also requiring an owner action logged as a THESIS launch precondition), 6 accepted as known limits now stated publicly in the deliverables (BUY-1, BUY-2, BUY-5, BUY-6, BUY-7 fact, BR-5 name risk), 0 rebutted outright, and 3 partially-fixed judgment calls logged in the disposition text (BR-2 aphorism ration, BR-5 no-rename decision, CAR-4 unpriced-not-deleted pricing).
The three kill-severity objections (SE-1, BUY-2, BUY-5) all resolve through the same mechanism: the content calendar is now hard-gated on the THESIS launch preconditions, which include fixing or annotating the broken benchmark and correcting the README before any post gets a date.

## The skeptical staff engineer

**Overall verdict (verbatim):** This package survives a hostile audit better than any launch package I have reviewed: I reran the numbers and they match to the third decimal, the 38 nightly snapshots were genuinely committed by github-actions[bot] on the dates claimed, every link I clicked resolves, the repo is public, the anti-claims discipline mostly holds through the derived copy, and the confession-arc centerpiece is checkable and true. What survives less well is the package's own covenant that it discloses everything first: the retrieval nightly's unexplained 18-day June outage, the E6 gate's fail-open behavior on transient errors, the deterministic-rerun framing that dresses 4 data points as 38, the unledgered founder biography, and the missing security-disclosure channel are all things the first serious skeptic will find and the known-weaknesses list does not contain. And one thing can actually kill it: the calendar dates a 07-20 launch while the README still asserts a green scale benchmark that has published 0.000 for 27 days - a receipts brand caught with one false receipt loses all of them, so the preconditions need enforcement, not acknowledgment. Fix the disclosure gaps and gate the calendar, and this launches as the rare marketing package that is telling the truth.

### SE-1 (kill)

> Your content calendar commits to a Monday 07-20 launch - four days out - while your own THESIS says the launch preconditions are RED, and the worst one is sitting in the README you drive every visitor to. README lines 43-58 still present the 10k-chunk scale table as all-1.000 and say, in the present tense, 'The nightly workflow fails loudly if the configured recall floor is breached' - while docs/permissions-scale-nightly has published 0.000 in every cell for 27 straight days. The landing page discloses it; the README, the first artifact any skeptic opens after clicking 'git clone the receipts', actively asserts the opposite. There is no annotation in the repo, no owner, and no date on any of the four preconditions, and nothing in this package structurally prevents the calendar from firing anyway. If this launches on 07-20 with the README as-is, the first Hacker News commenter finds the contradiction in five minutes and the entire 'receipts' brand is dead on arrival - a receipts brand caught with one false receipt loses all of them. And the meta-problem is worse than the bug: your centerpiece story says the July fix taught you that an ignored red alarm is the failure mode, yet the scale benchmark's alarm has been firing into a void for four weeks INCLUDING the entire week you spent writing marketing copy about having learned that lesson.

*Evidence cited:* Read README.md:40-58 ('Every cell is 1.000... The nightly workflow fails loudly'); docs/permissions-scale-nightly/2026-07-15.md (0.000 every cell); THESIS.md launch preconditions ('currently RED, must be green before any public launch'); launch/social/content-calendar.md ('Dates assume a Monday 2026-07-20 launch'). Grepped the repo for any breakage annotation: none exists outside launch/.
*Suggested fix:* Block the calendar on the preconditions mechanically: either fix the benchmark or land a dated breakage annotation in README.md itself (not just the landing page) before any date is put on any post. Add an owner and a date to each precondition in THESIS.md.

**Disposition:** **Fixed (in-package) + owner-gated.** The calendar now opens with a hard gate: no real dates until every THESIS precondition is green (`launch/social/content-calendar.md` header). THESIS preconditions now carry named actions and done-when conditions, with the README annotation as precondition 2. The README edit itself is outside this build's write scope (guardrail: do not modify application source or docs outside launch/); it is precondition 1-2, owner-owned, and the calendar cannot fire before it.

### SE-2 (major)

> The retrieval nightly - the artifact you call 'the public record' - silently stopped publishing for 18 days, June 1 through June 18, while its sibling permissions-scale workflow committed every single night of that window. I checked git: 14 'permissions scale nightly' commits in that span, zero 'nightly retrieval eval' commits, resuming 06-19. So the series you are marketing as the always-on public record has already had a two-and-a-half-week unexplained outage, nobody noticed that either, and not one deliverable - not the THESIS known-weaknesses list, not claims.md A9 (which says 'history has gaps' and stops there), not the site - says WHY. Your own kill criterion 4 is 'the nightly series stops publishing.' It already happened, pre-launch, and the package's answer to the inevitable 'what happened in June?' question is nothing. For a package this obsessed with pre-empting skeptics, leaving the single most visible anomaly in your flagship artifact unexplained is a hole a staff engineer falls into on day one.

*Evidence cited:* Ran: git log --format="%ad %s" --since=2026-06-06 --until=2026-06-20 --all | grep -c 'permissions scale' -> 14; same window grep 'retrieval eval' -> only 06-19 and 06-20. docs/nightly/ listing jumps 2026-05-31 -> 2026-06-19. claims.md A9 and J7 note the gap's existence but no deliverable explains its cause.
*Suggested fix:* Find the actual cause (workflow run logs / actions history), add one honest sentence to the site arc and a row to the known-weaknesses list. 'The harness itself was down for 18 days and I did not notice that either' is perfectly on-brand - but only if you say it first.

**Disposition:** **Fixed.** Cause diagnosed from the GitHub Actions API: the nightly RAN and FAILED at its 'Seed corpus + run eval' step for 18 consecutive nights (06-01..06-18), resuming 06-19. Disclosed in: claims A9 (rewritten), THESIS weakness 9, the site arc and honest section, launch-thread tweet 10, IG-06 card and caption, and datasheet erratum PV-E9. Said first, as suggested.

### SE-3 (major)

> Your zero-leak gate fails open on infrastructure failure, and no deliverable says so. I read evals/retrieval/runner.py: when E6 exhausts its retries on a 'transient' error (429, network reset, 5xx, DB hiccup), it sets e6_error, prints 'NOT RUN - non-blocking', and the run exits 0 - a green CI check on a PR where the cross-workspace leak eval never executed. Combine that with no branch protection and a solo maintainer and the gap is real: a sustained rate-limit day plus a retrieval-predicate change merges green with the security eval skipped. Meanwhile the site says 'No security eval has a config off-switch' and 'Attacked, not just asserted', the datasheet lists XWS_max as an enforced rating, and your own CLAUDE.md invariant says 'fail-CLOSED where a miss leaks.' The design is defensible and the code comments defend it thoughtfully - but the marketing sells an absolute ('silencing one is a code-visible PR diff') while the runner contains a silencing path that requires no diff at all, just a flaky afternoon. That asymmetry between copy and code is exactly what this package promises not to have.

*Evidence cited:* evals/retrieval/runner.py:1785-1799 (E6ExecutionError -> 'the merge is not blocked') and runner.py:1895-1899 ('NOT RUN - transient execution error (non-blocking)', no exit-1 path for e6_error); evals/retrieval/e6.py:64-77 docstring. Contrast: launch/site/index.html 'No security eval has a config off-switch'; extras/datasheet.html XWS_max row; THESIS known-weaknesses list omits it.
*Suggested fix:* Either make repeated E6 execution failure fail the check (fail-closed, retry the job), or disclose it: add it to the known-weaknesses list and soften the 'no off-switch' copy to name the not-run case ('a leak verdict cannot be downgraded; an eval that cannot execute is flagged loudly but does not block').

**Disposition:** **Fixed (copy) + owner decision logged.** All 'no config off-switch' claims are now scoped to the verdict, with the not-run path disclosed: site (permissions bullet + honest section), THESIS (asset section + weakness 10), claims B8 (scope limit added), datasheet (XWS row + new erratum PV-E8), X-03 reworded. Making repeated execution failure fail-closed is flagged to the owner as an open decision; the eval code itself is outside this build's write scope.

### SE-4 (major)

> The package repeatedly dresses deterministic repetition up as accumulating evidence. I pulled keyword recall@5 from all 38 JSONs: the series is 0.11 x 30, 0.14 x 3, 0.9167 x 5 - identical to four decimal places night after night, because it is the same deterministic harness rerunning the same 60 questions against the same unchanged 16-chunk corpus. So '180 no-access runs a night' (IG-04's headline and the site's closing line), 'the zero-leak table has read 1.000 in every cell in every published snapshot' (L-02), and the trail chart all imply longitudinal evidence when the honest information content of 38 snapshots is about four distinct data points. The nightly cadence proves the pipeline runs, not that anything new is being tested. The confession arc survives this - the record's value there is that it existed and was ignored - but the security copy leans on snapshot-count as if N=38 were independent trials. Any engineer who opens two JSONs and diffs them will see this in thirty seconds, and your ICP is precisely the engineer who diffs the JSONs.

*Evidence cited:* Extracted aggregates.keyword.overall.recall_at_5 from every docs/nightly/*.json: constant 0.11 (05-19..07-06), 0.14 (07-07..07-09), 0.9167 (07-11..07-15); 07-11 and 07-15 headline tables byte-identical. Copy: launch/social/instagram/posts.md IG-04, launch/social/linkedin/posts.md L-02, launch/site/index.html About section ('180 no-access runs a night').
*Suggested fix:* Reframe the nightly as what it is - a change-detector and an audit trail ('the same assertions, rerun every night, so any code change that breaks them is dated to the day') - and drop phrasings that treat snapshot-count as sample size.

**Disposition:** **Fixed.** Copy no longer treats snapshot count as sample size: claims A9 gains the change-detector framing note and A14 ledgers the 180-run derivation as per-run arithmetic; the site eval section and About line, L-02, IG-04 (card headline re-rendered + caption), and T-02 all now say 'per nightly run' / 'audit trail, not independent trials.'

### SE-5 (major)

> The most credibility-load-bearing claims in the whole package have no claim ID and no evidence file: the founder biography. The site About says 'ten years in hardware validation and two in applied AI consulting'; L-04 names an employer ('At Zebra I validated hardware'); the LinkedIn bio repeats 'ten years of hardware-validation background.' Your THESIS's first paragraph promises 'anything without an ID is labeled as inference or assumption,' the site footer taunts 'This page makes no claim without a path,' and GUIDELINES.md §5 orders 'do not write new factual claims without adding them to that ledger first' - yet the ledger has no section for the founder, and I can find nothing in the repo or evidence/sources/ substantiating any of it. This copy is aimed at hiring managers; if any detail is off - years miscounted, employer name wrong, 'consulting' inflated - it is the founder's professional reputation that takes the hit, in front of exactly the audience the job search needs. The package red-teamed every number except the ones attached to a person.

*Evidence cited:* launch/site/index.html #about section; launch/social/linkedin/posts.md L-01 ('I spent ten years in hardware validation') and L-04 ('At Zebra'); launch/extras/profile-assets/bios.md. Grepped launch/evidence/claims.md: no bio/founder claim IDs exist (sections A-J cover repo, competitors, pricing, naming only).
*Suggested fix:* Add a section K to claims.md for biographical claims, have hcho verify each against his own resume/LinkedIn, and strike any detail (especially the employer name) he does not want permanently welded to this product.

**Disposition:** **Fixed (ledgered) + owner verification required.** New claims section K: the bio figures are ledgered as assumed (owner-supplied via the mission brief), the site now says 'about ten years' (matching the brief's '~10'), and verification against the resume is THESIS launch precondition 5. The build cannot verify a resume it does not have; it can and did stop the copy from claiming more than its source.

### SE-6 (major)

> A product whose entire pitch is 'my security properties are attacked in CI' ships with no SECURITY.md and no private vulnerability-disclosure channel - and the site's footer actively routes discoveries to a PUBLIC issue ('Found one? Open an issue titled claim without a receipt'). Walk the scenario: a stranger reruns your harness, finds an actual cross-tenant leak vector, and does exactly what your footer says - now there's a public GitHub issue documenting a live leak in the permissions-aware RAG kit, timestamped before any fix. That is the single worst artifact that could exist under this brand, and the package currently instructs people to create it. Every serious security-adjacent repo ships a disclosure policy; the absence is also a tell to the exact buyer you want that the security posture is eval-deep, not operations-deep. This is an undisclosed weakness your known-weaknesses list should have caught: the list covers eval scope and history thinness but says nothing about how a real vulnerability report would be handled.

*Evidence cited:* Ran: ls /Users/hcho/Developer/Purvia/SECURITY.md -> No such file or directory; no .github/SECURITY.md either. launch/site/index.html footer: 'Found one? Open an issue titled "claim without a receipt."' THESIS.md known-weaknesses list (items 1-8) has no disclosure-process entry.
*Suggested fix:* Commit a SECURITY.md with a private reporting channel (GitHub private vulnerability reporting or an email) before launch, and change the footer line to route claim disputes to issues but security findings to the private channel.

**Disposition:** **Fixed (footer) + owner action.** The site footer now routes security findings to private vulnerability reporting, never a public issue; the datasheet footer says the same. Committing SECURITY.md and enabling private reporting are THESIS precondition 6 (outside this build's write scope).

### SE-7 (minor)

> The mandated house phrase 'the leg stayed dead while the figure drifted 0.110 to 0.140' misdescribes what the git history shows. The step to 0.140 appears in the 07-07 nightly, and the commits immediately preceding it are the golden-set rewrite - PR #67 (content-anchor gold-label resolver) and PR #68 (layered golden set) on 07-06. No retrieval code changed. The measuring instrument changed; the leg did not 'drift.' It's a small word, but GUIDELINES.md §4 bakes it into a mandatory formulation, the site says '0.140 by July 9,' and IG-06 narrates 'a drift to 0.140' over a chart captioned 'values plotted verbatim.' A skeptic who lines the series up against git log - which took me one command - will read 'drift' as either sloppiness or smoothing, and on a receipts brand you don't get to be either.

*Evidence cited:* Keyword recall series: 0.11 through 2026-07-06, 0.14 from 2026-07-07. git log 07-04..07-08: 25e2480 (#67) and 83b5eab (#68) land 07-06, nightly 16f9b8a publishes 07-07. launch/brand/GUIDELINES.md §4 house phrasings; launch/social/instagram/posts.md IG-06 caption.
*Suggested fix:* Change the house phrase to name the cause: 'the figure moved 0.110 to 0.140 when the golden set was extended on 07-06; the leg stayed dead throughout.'

**Disposition:** **Fixed.** The instrument change is now named everywhere the 0.140 appears: house phrase rewritten ('the figure moved 0.110 to 0.140 when the golden set was extended on 07-06'), GUIDELINES hero and message 2, site hero and arc, IG-01/IG-06 captions.

### SE-8 (minor)

> Your own anti-claims list contradicts your mandatory differentiator sentence. J14 bans 'blocks the merge / merge-blocking' for Purvia's gates because main has no branch protection - yet the J13/E10 house phrase, which GUIDELINES says to use 'all three qualifiers, always,' is 'no comparable open kit we found ships chunk-level ACLs in the retrieval predicate with CI-blocking leak evals,' and THESIS line 45 deploys it. So the one sentence positioning Purvia against the field ascribes to Purvia the exact property ('CI-blocking') the ledger admits it does not literally have yet. A pedant - and you are marketing exclusively to pedants - will notice the package polices 'fails the required CI check' everywhere except in its most-repeated competitive claim.

*Evidence cited:* launch/evidence/claims.md J14 ('Say "fails the required CI check"') vs E10/J13 approved phrasing ('CI-blocking leak evals... all three qualifiers, always'); THESIS.md line 45; launch/brand/GUIDELINES.md §4 house phrasings list both, four lines apart.
*Suggested fix:* Change the E10 phrase's third qualifier to 'leak evals that fail the required CI check' everywhere, or enable the branch-protection ruleset (precondition 2) before first use and make 'CI-blocking' literally true.

**Disposition:** **Fixed.** The E10/J13 house phrase's third qualifier now reads 'leak evals that fail the required CI check' in GUIDELINES, THESIS, and the claims ledger; 'CI-blocking' is gone from every deliverable.

### SE-9 (minor)

> The public repo's working tree currently contains the entire marketing sausage factory as untracked files: gtm/marketing-build-prompt_071626.md (the build prompt for this campaign) and the whole launch/ directory including BUILD_LOG.md and the forthcoming RED_TEAM.md, plus an uncommitted edit to frontend/widget-host-example.html swapping the demo placeholder for a real minted widget key. One reflexive 'git add -A && git commit && git push' - or any agent doing cleanup - publishes the prompt that generated the first-person confession narrative into the very repo the narrative points at. The key is non-secret by your own invariant 3, so that part is cosmetic, but the prompt file is not: 'solo engineer's hard-won honesty' reads very differently next to the orchestration script that manufactured it. This is a one-command-from-disaster hygiene state in the highest-stakes directory you own.

*Evidence cited:* git status in /Users/hcho/Developer/Purvia: 'M frontend/widget-host-example.html', untracked gtm/, launch/, supabase/snippets/ ('Untitled query 172.sql' etc.). ls gtm/ -> marketing-build-prompt_071626.md. git diff shows data-public-key wk_pk_demo_example -> wk_pk_-qCHpH6kbt-yEJLlQob0OOX3qFPgxPID.
*Suggested fix:* Add gtm/ and launch/ to .gitignore (or move them out of the repo checkout entirely), revert or commit the widget-host-example.html edit deliberately, and clean supabase/snippets/.

**Disposition:** **Fixed (hygiene) + owner note.** `gtm/` and `launch/` are now in `.gitignore` (they no longer appear as untracked), which this build judged within scope as a publish-guardrail safety measure; BUILD_LOG Q6 records the reasoning. The uncommitted `frontend/widget-host-example.html` edit predates this build and is flagged to the owner (THESIS precondition 7) rather than reverted.

### SE-10 (minor)

> The site hands skeptics evals/retrieval/e6.py as a primary receipt, and the first thing its docstring tells them is stale: '~300 live calls (3 modes x 50 questions x 2 passes)' - the golden set has been 60 questions since 07-10, so it's ~360. Datasheet erratum PV-E7 proudly catalogs stale comments (the nightly workflow's '50-question', ship-green's '7-doc/14-chunk') but missed the one in the file your own landing page links as evidence. Trivial, but the package's whole schtick is that the artifacts are more careful than the copy; here the copy is more careful than the artifact it cites.

*Evidence cited:* evals/retrieval/e6.py:64 ('3 modes x 50 questions x 2 passes'); grep -c 'id:' evals/retrieval/retrieval_gold.yaml -> 60; launch/extras/datasheet.html PV-E7 lists only the workflow and ship-green comments; launch/site/index.html links e6.py in the Permissions section.
*Suggested fix:* Fix the docstring in the same pre-launch cleanup PR PV-E7 already promises, and add e6.py to the PV-E7 row.

**Disposition:** **Fixed (datasheet) + owner cleanup.** Datasheet erratum PV-E7 now includes the e6.py docstring's stale '50 questions.' The docstring itself is eval code, outside this build's write scope; it rides the same cleanup task PV-E7 already tracks.

### SE-11 (minor)

> The P1b non-disclosure eval is sold harder than its design earns. X-05 calls it 'an eval assertion, not a code comment' and the datasheet rates ESC_disc at '0 bytes beyond the generic deferral' - but I read assert_p1b_non_disclosure: both the reference bytes and every probe row are produced by the same _escalated constructor in backend/escalation.py. The assertion proves that ONE constructor is invariant to the escalation reason - a real regression class, and the injectable output-fn shows you thought about it - but it cannot see a production call-site divergence: if the live SSE path in main.py ever composes or decorates the customer-facing message separately from that constructor, the eval stays green while production discloses. Byte-equality of the constructor's output is necessary, not sufficient, for 'escalating never discloses that restricted content exists,' and the copy states the sufficient version.

*Evidence cited:* evals/retrieval/e7_runner.py:1535-1562 (reference and probe both 'taken through the REAL escalate constructor'; p1b_output_fn injection point); launch/social/x/posts.md X-05; launch/extras/datasheet.html ESC_disc row.
*Suggested fix:* Either add an assertion that the widget response path emits the constructor's bytes unmodified (pin the call-site, not just the constructor), or scope the copy: 'the escalation constructor is asserted byte-identical across reasons.'

**Disposition:** **Fixed.** X-05 and the datasheet ESC_disc row now state the precise scope: the byte-equality assertion pins the escalation constructor's output across reasons; a call-site that composed the message separately would be outside its sight. Pinning the call-site is noted as the stronger fix, owner-side.

## The buyer

**Overall verdict (verbatim):** This is the most honest marketing package I have ever red-teamed, and that is exactly why I am not buying: the disclosures, read as a buyer instead of an admirer, assemble themselves into the case against adoption. The proof does not transfer - every receipt was measured on 8 documents and dies by design when I swap in my corpus, leaving me the golden-set authoring burden the pitch claims I cannot cheaply build. The stack silently requires Supabase as my identity platform, the 'raw SDK' install ships torch, and the only benchmark near my scale has been publishing 0.000 daily for a month while the maintainer built Instagram cards - with the fix listed as 'workaround: none' on his own datasheet and the red workflow conspicuously absent from the site's badge row. There is nothing to pay for (the pro tier is a self-described empty hypothesis), no one else using it, no branch protection, and a thesis document whose failure modes all terminate in 'the job search keeps the technical story.' As engineering-portfolio evidence this repo is genuinely excellent, and I will happily steal its eval methodology from the MIT source - which is precisely the problem: the package teaches me everything I need in order to not need it.

### BUY-1 (major)

> Your own site's proof section is my exit ramp from ever adopting this. The landing page prints the entire match_chunks predicate with a link to the migration, cites the Supabase RLS-with-vector-search tutorial as validated architecture, and MIT-licenses e6.py, the AU4 suite, and the golden-set authoring guide. I'm a 4-person team; I will do exactly what your page teaches me to do: read the predicate, read golden-set-authoring.md, and port the pattern into my own stack in two days without ever cloning your kit, starring the repo, or showing up in your funnel. The pitch says 'what you will not cheaply build is the proof' - but you just published the recipe for the proof, for free, as the marketing. The value prop is self-cannibalizing: your best-fit buyer is precisely the person most capable of extracting 100% of the value from the receipts without becoming a user.

*Evidence cited:* launch/site/index.html lines 151-163 (full predicate + migration link), lines 226-231 (golden-set authoring linked); THESIS.md 'What they do today' item 2 and claims G6 (Supabase RLS tutorial cited as the DIY alternative); docs/golden-set-authoring.md teaches the complete labeling contract; LICENSE is MIT; F2 in claims.md admits this buyer's creed is 'you don't need a framework.'
*Suggested fix:* Reframe the buy decision as time-to-verified rather than capability: quantify what porting the harness actually costs (the anchor resolver, the derived permission matrix, E6 seeding, the gate loader) versus cloning it, and make the landing page sell the integrated harness, not the excerpt.

**Disposition:** **Accepted as the central bet risk, partially mitigated.** Publishing the recipe is the deliberate credibility strategy; whether receipts convert instead of substituting is exactly what THESIS kill criteria 1-2 measure, and the buyer's 'teaches me everything I need in order to not need it' is quoted there in spirit. Mitigation applied: the site's honest section now states the day-one authoring cost plainly, so the time-to-verified trade is explicit rather than discovered.

### BUY-2 (kill)

> Every receipt on this page evaporates the moment I point the kit at my corpus. The 1.000 zero-leak table, the 0.950 recall, the 38 nightlies - all measured on 8 documents and 16 chunks, and by your own design 'replacing the corpus and authoring your own golden set are the same step.' So my day one looks like: zero receipts, hand-author 60 questions with byte-exact verbatim anchors (your guide warns a smart-quote or an em dash typed as a hyphen fails the run), and take on a labeling contract where 'under-labeling is a security defect' - meaning the security guarantee I'm supposedly buying is now bounded by MY labeling diligence, not your code. That is the exact DIY eval-building burden your thesis says I can't cheaply build, handed back to me as a prerequisite. The receipts are non-transferable; what transfers is homework.

*Evidence cited:* launch/site/index.html line 229 ('replacing the corpus and authoring your own golden set are the same step, by design') and line 263 (8 docs / 16 chunks); docs/golden-set-authoring.md ('a worked example to learn the format from, not a survives-the-swap artifact'; §3 'under-labeling is a security defect'; exact-match anchor rules); datasheet erratum PV-E3; claims B6/J15 scope the 1.000 table to gold labels.
*Suggested fix:* Ship a bridge: a semi-automated golden-set bootstrapper (draft questions + anchors from the buyer's corpus for human review) and publish a measured 'time to first green run on your own corpus' number from at least one real external attempt. Until then, the site should state the day-one authoring cost as plainly as it states the metrics.

**Disposition:** **Accepted + disclosure strengthened.** The receipts are non-transferable and the package now says so in the buyer's face: site honest section rewritten ('day one on your data starts at zero receipts plus a labeling contract'), THESIS weakness 11 added, and the semi-automated golden-set bootstrapper is logged as the roadmap answer (not shipped, said as such).

### BUY-3 (major)

> Nowhere on the site does it say the sentence that disqualifies me: 'Purvia requires Supabase as your application's auth provider.' The entire security story is auth.uid() resolved from a GoTrue JWT under SECURITY INVOKER, chunk_acl and workspace_membership tables, 45 Supabase migrations, a service-role key, and PostgREST. My startup has real users on our own auth today. Adopting this means either migrating my production identity system into GoTrue, or rewriting the predicate, the JWT minting, the RLS policies, and every eval seed against my own auth - at which point I have rebuilt the Supabase tutorial your own evidence file cites as the free alternative. 'The buyer trades vendor operations for owning the stack' is the line you use against Vectara; it cuts against you too, and the datasheet's operating-conditions table lists Supabase like it's a neutral choice of Postgres host rather than the load-bearing identity platform.

*Evidence cited:* launch/site/index.html permissions section (auth.uid(), SECURITY INVOKER, workspace_membership); supabase/migrations/20260617120300_match_chunks_workspace_membership.sql; README env table (SUPABASE_URL/ANON_KEY/SERVICE_ROLE_KEY/JWT_SECRET all load-bearing); datasheet §2 lists 'Supabase local or hosted' as the only Postgres row; claims G6 cites supabase.com/docs/guides/ai/rag-with-permissions as the DIY path; no page in launch/ states the auth-provider requirement.
*Suggested fix:* Add an explicit 'Assumptions' block to the site and datasheet: Supabase is the identity and RLS substrate, migration paths for existing-auth apps are out of scope (or document the port surface honestly).

**Disposition:** **Fixed.** The Supabase-as-identity-substrate requirement is now stated where a buyer will see it: site honest section (adoption assumptions bullet), datasheet operating conditions (rewritten row), THESIS weakness 12, claims L1.

### BUY-4 (major)

> The tagline is 'raw OpenAI SDK + Pydantic; no orchestration framework in the product,' and then backend/requirements.txt hands me docling, which your own comments admit pulls torch transitively and forces a numpy<2 pin because torch<2.4 can't initialize against numpy 2.x. Your own CI splits AU4 into a separate job specifically 'because it imports backend/main.py (docling/torch).' So the lean-stack kit ships a multi-gigabyte ML runtime in the default install of a support widget backend, and even the maintainer's own pipeline routes around the weight. 'No framework' was supposed to mean I understand and own every dependency; instead my `pip install -r requirements.txt` cold-start and my Docker image are dominated by a parser I may never use.

*Evidence cited:* backend/requirements.txt (docling>=2.20.0,<3 with the torch/numpy<2 comment block, pypdfium2 pin); README eval-suite section: AU4 is 'its own job because it imports backend/main.py (docling/torch)'; launch/site/index.html hero: 'no orchestration framework in the product.'
*Suggested fix:* Make docling an optional extra (parsing already sits behind the PARSER seam per ADR-0007 and backend/test_parsing_seam.py) so the core install is actually the raw-SDK footprint the copy sells, and state the install weight on the datasheet's operating conditions.

**Disposition:** **Fixed (disclosure) + roadmap.** The docling/torch install weight is disclosed on the site (adoption assumptions), the datasheet (new install-weight row), THESIS weakness 12, claims L2. The optional-extra split behind the existing PARSER seam is logged as a roadmap item; changing requirements.txt is outside this build's scope.

### BUY-5 (kill)

> The one benchmark at anything resembling my data size - the 10k-chunk permissions-scale nightly - has published 0.000 in every cell since June 19. I checked the Actions API myself: the workflow failed on July 14, 15, and 16, the morning of this launch package's date. That is four weeks of a red security-adjacent benchmark, with the 'regression alarm' firing into a void daily, while in the same window the maintainer shipped a rebrand, a landing page, a datasheet, four social channels, and rendered Instagram cards. For a kit whose entire pitch is 'the receipts are maintained with the same discipline as the feature list,' the revealed priority ordering is marketing over fixing the broken receipt. Your own thesis calls this a RED launch precondition and then the package around it got finished anyway. As a maintenance signal for a solo-maintainer dependency I'd be putting under real users, this is disqualifying: honest disclosure of neglect is still neglect.

*Evidence cited:* Ran `gh api repos/hcho22/Purvia/actions/workflows/permissions-scale-eval.yml/runs` - conclusions: failure on 2026-07-14, 07-15, 07-16; docs/permissions-scale-nightly/2026-07-15.md shows 0.000 in every cell; THESIS.md launch precondition 1 ('currently RED... has been since 2026-06-19'); datasheet erratum PV-E1 workaround column literally says 'none'; git log shows rebrand PRs #87-#92 merged 07-15/16 while the benchmark stayed red.
*Suggested fix:* Do not publish anything until the scale benchmark is green or the repo carries a dated, committed breakage annotation with a fix ETA. The fix (re-parenting the benchmark's synthetic viewers after the workspace-membership migrations) should cost less than one of the eight Instagram cards did.

**Disposition:** **Accepted, and converted into a hard gate.** The objection is correct: honest disclosure of neglect is still neglect. The package's answer is structural now: fixing or annotating the benchmark is THESIS precondition 1, the README correction is precondition 2, and the content calendar is hard-gated on all preconditions; no post gets a date while the benchmark is red and unannotated.

### BUY-6 (major)

> The repo's front page contradicts the repo's own nightly directory, today. README shows the 10k-chunk sweep as an all-1.000 table, undated, followed by 'The nightly workflow fails loudly if the configured recall floor is breached' - present tense, as if it's running green. Two clicks away, docs/permissions-scale-nightly/2026-07-15.md publishes 0.000 in every cell of the same table, as it has for a month. Your landing page carefully says the HNSW findings are 'design-time results, dated June 2026,' but the README - the first thing any buyer actually reads, and the thing your claims ledger J2 was written to police - carries no date and no annotation. The launch copy is more honest than the product's own front door, which means the honesty is in the marketing layer, not the artifact. First discrepancy I find between your table and your receipts, the whole 'rerun my numbers' trust model is dead.

*Evidence cited:* README.md lines ~43-58: all-1.000 viewer/ef_search table with no date and 'fails loudly' claim; docs/permissions-scale-nightly/2026-07-15.md: 0.000 every cell; claims.md J2 forbids citing the README table as current, yet the README itself still presents it that way; launch/site/index.html line 261 adds the 'design-time results, dated June 2026' qualifier the README lacks.
*Suggested fix:* Before launch, edit the README table in place: date it, mark the benchmark BROKEN since 2026-06-19 with a link to the red nightly directory. The repo must be at least as honest as the landing page about the repo.

**Disposition:** **Accepted + owner-gated.** The README contradiction is real and this build may not edit the README (guardrail). It is now THESIS launch precondition 2 with the exact edit specified (date the table, mark the benchmark broken since 06-19, link the red directory). The landing page cannot launch before the repo's front door matches it.

### BUY-7 (major)

> Bus factor is the whole ballgame for a security-critical dependency, and every observable says one person, zero community, exit-ramp ready. I checked: 0 stars, 0 open issues, every 'issue' is a self-merged PR, no branch protection, not deployed anywhere. The thesis document itself says the quiet part: kill criterion 1 gives this 30 days post-launch to produce receipt-citing inbound, and the failure action is 'keep the repo as a portfolio artifact; the job search keeps the technical story.' Kill criterion 2's failure action redirects to 'consulting or employment leads from the same artifact.' Launch precondition 4 routes the package through staff-engineer and hiring-manager reads before a single buyer sees it. So the founder's own written plan treats my adoption as one branch of a job search, with a 30-day fuse. If I build my permissions layer on this and he's hired in Q4 - his own most-planned-for outcome - I own an unmaintained fork of 27,000 lines of someone else's Python. Nothing on the site addresses succession, and 'you would be early' is doing a lot of load-bearing understatement.

*Evidence cited:* Ran `gh api repos/hcho22/Purvia`: private=false, stars=0, open_issues=0, all issues are closed self-merged PRs; site index.html #honest ('Solo repo... no branch protection'; 'Zero users... You are early'); THESIS.md kill criteria 1-2 ('the job search keeps the technical story', 'employment leads') and launch precondition 4 (hiring-manager reads); claims A6 (PRs self-merged), J14 (no branch protection, GitHub API 404).
*Suggested fix:* Address it head-on in the site's honest section: the maintenance commitment window, what happens to the repo if the maintainer takes a job, and why MIT + the eval harness makes a hard fork survivable. Silence here reads as the THESIS's exit plan showing through.

**Disposition:** **Fixed (disclosure); the underlying fact stands.** The site's honest section now addresses succession head-on: bus factor is one, and if the maintainer takes a job what survives is MIT code plus the eval harness that lets a fork prove its properties. THESIS weakness 13 records that the kill criteria route failure into the job search; the package no longer leaves the exit plan to be discovered.

### BUY-9 (major)

> The hero shows three live GitHub Actions badges and the line 'If one is red, that is the product working.' Cute - except the repo has six workflows and the one that has been red every single day for a month, permissions-scale-eval.yml, is exactly the one missing from the badge row. I diffed the badge list against `gh api .../actions/workflows` myself. Curating your badge strip down to the green ones, on a page whose entire brand is radical receipt honesty, is the precise move the page claims you never make. Yes, the breakage is disclosed in prose four sections down; the badge row is what people screenshot. The first commenter who notices this on HN ends the launch narrative in one reply.

*Evidence cited:* launch/site/index.html lines 96-101: badges for retrieval-eval.yml, ship-green.yml, retrieval-eval-nightly.yml only, plus 'If one is red, that is the product working'; ran `gh api repos/hcho22/Purvia/actions/workflows` - six active workflows including Permissions scale eval; its last three runs (07-14/15/16) all concluded failure.
*Suggested fix:* Add the permissions-scale-eval badge, red as it is, next to the others - or delete the 'if one is red' line. Showing the red badge is the single cheapest credibility purchase available in this package.

**Disposition:** **Fixed.** The permissions-scale-eval badge, red, is now the fourth badge in the hero row, and the 'if one is red, that is the product working' line stays because it is now true on the page. Cheapest credibility purchase in the package, as suggested.

### BUY-8 (minor)

> I ran your 'Run it, then check my numbers' block as written and it falls over on line three: the site's app quickstart goes `cp .env.example .env` straight to `uvicorn main:app` with no `pip install -r requirements.txt`, and skips the frontend's `cp .env.example .env` that the README requires. The README has both steps; the page that promises 'every claim on this page ends in a file path you can open' abbreviated the one block buyers will actually paste into a terminal. On a receipts brand, the reproduction instructions are a claim, and this one is wrong verbatim.

*Evidence cited:* launch/site/index.html lines 239-242 (four-line quickstart, no pip install, no frontend .env) vs README.md Local development (steps include `pip install -r requirements.txt` and frontend `cp .env.example .env`); backend/.env.example exists, confirming the README flow is the real one.
*Suggested fix:* Copy the README's command block verbatim, including the pip install and both .env copies, and note the eval block's required env vars (CORPUS_SEED_DATABASE_URL, service-role key, OpenAI key).

**Disposition:** **Fixed.** The site quickstart now matches the README verbatim flow: `pip install -r requirements.txt` and the frontend `cp .env.example .env` are in the block.

### BUY-10 (minor)

> 'The support bot's risk tolerance is a number' - but for my deployment that number is unenforced until I build the enforcement myself. The ceiling is checked only by a weekly LLM-judged eval against an escalation golden set, so on my corpus the 5% means nothing until I author my own escalation gold, stand up the weekly workflow, and pay for an Anthropic judge key - and your own datasheet says the thresholds feeding the gate (tau_sim 0.4, n_min 2, cutoff 0.7) are untuned placeholders with exactly zero escalation-weekly reports ever published, and one RAGAS snapshot dated May 21. The datasheet dresses this up as an 'absolute maximum rating' in a table styled after silicon datasheets; a real datasheet's abs-max ratings were measured on the shipped part. FRmax was never measured on any part, including yours.

*Evidence cited:* datasheet.html §3 (FRmax as absolute maximum rating) vs errata PV-E5 (placeholder thresholds) and PV-E6 (no escalation-weekly reports); claims C2-C4, J6, J12; README eval-suite section (weekly E7 sweep needs ANTHROPIC_API_KEY; docs/escalation-weekly/ does not exist per J6); site index.html escalation caveat bullet confirms 'published track record is short.'
*Suggested fix:* Rename the table row to 'enforcement point, unmeasured' or publish at least 4-6 weekly escalation reports before using abs-max framing; state the buyer-side cost of the ceiling (own escalation gold + judge key + weekly run) next to the number.

**Disposition:** **Fixed.** The datasheet FRmax row now says 'enforcement point, not a measured characteristic: no weekly report has published yet (PV-E6)' and names the buyer-side cost (own escalation gold plus a judge key). The abs-max framing survives only with that caveat attached.

## The brand critic

**Overall verdict (verbatim):** This is, by a wide margin, the most disciplined launch package I have reviewed in 2026 - the claims ledger with an anti-claims section, the datasheet, and the honest red cells are moves the transparency genre talks about and almost never executes - and that is exactly why its failures sting: they are all failures of the package against its own published rules. The voice rules are broken by the launch thread itself (rhetorical-question openers, stacked one-liners, eight of ten X posts over the stated length), the banned hype vocabulary has been replaced wholesale by fourteen coined aphorisms that read as keynote rather than code review, the copy narrates its own honesty seven times while the calendar schedules 'the required weakness post' for conversion, the green-means-verified system is violated by the hero CTA and the flagship chart card, the name encodes the permissions feature while the tagline sells the receipts thesis and googles to a face-swap app, and the redemption arc's 'observable forever' moral ships alongside a second, live, identical alarm-into-the-void failure. Nothing here is unfixable, and none of it touches the underlying asset, which is real; but a receipts brand is graded on the delta between what it preaches and what it ships, and right now that delta is visible to any reader as careful as the one this package is explicitly courting. Fix the self-narration, the semantic green, the banner caveat, and the dead datasheet citation before a single pixel publishes, publicly accept or resolve the name, and this launches as the rare honest entrant in a genre drowning in performed honesty - ship it as-is and the genre eats it.

### BR-1 (major)

> The confession is real; the performance of the confession is the problem. This package cannot stop narrating its own honesty: 'That is the most honest sentence in this launch' (T-04), 'Credibility is bought with disclosed weaknesses' (IG-08 caption), 'The honesty section is a feature' (ig-08 card), 'so you hear it from me first' (thread 10/10), 'I will say it before you ask' (IG-04), 'The part of the launch post nobody writes' (IG-08), and a calendar that schedules 'the required weakness post' for Wednesday of week 1 because, in your own words, 'it converts thread readers into believers or filters them early.' Vulnerability with a conversion rationale and a calendar slot is strategy wearing candor's clothes, and in 2026 every third dev-tool launch is a postmortem-as-launch under a #buildinpublic hashtag - a hashtag you also use, three times. The receipts genuinely distinguish you from that genre; the self-congratulation dissolves the distinction. One reply - 'the I'm-so-honest bit is the new growth hack' - and the whole arc reads as shtick. Confess once, on launch day, and never again narrate your own candor. Let the red cells talk; they are the only ones who can say this without it curdling.

*Evidence cited:* launch/social/tiktok/scripts.md T-04; launch/social/instagram/posts.md IG-04/IG-08 captions; launch/social/instagram/cards/ig-08.png headline; launch/social/x/launch-thread.md tweet 10; launch/social/content-calendar.md Wed 07-22 row (verbatim: 'it converts thread readers into believers or filters them early'); #buildinpublic hashtags on IG-01, IG-06, IG-08; launch/social/x/posts.md X-09 ('which is its own lesson').
*Suggested fix:* A de-narration pass: delete every sentence that praises the disclosure it sits next to (the seven quoted above are the full list). Keep the disclosures. Run the confession arc once (X thread + L-01); after that, reference it with a path, not a retelling.

**Disposition:** **Fixed.** De-narration pass applied: all seven self-praising sentences removed or rewritten (T-04 'most honest sentence' line, IG-08 caption and card lead, thread 10/10 'so you hear it from me first', IG-04 'before you ask', IG-08 'the part nobody writes', X-09 'its own lesson'); the calendar rationale no longer sells the weakness post as a conversion device; #buildinpublic removed from all three IG posts. The confession arc runs once per channel and is capped by the calendar's new confession budget (see CAR-3).

### BR-2 (major)

> You banned the hype words and the hype came back as aphorisms. I counted fourteen coined maxims in one package: 'an alarm that always fires is no alarm,' 'publication is not detection,' 'a validation record you can edit is not a record,' 'the harness cannot make you look, it makes the failure undeniable,' 'if your KPI can be written by the thing it measures, it is not a KPI,' 'a receipt behind a paywall is not a receipt,' 'the proof is the pitch,' 'the bug cost one migration, the alarm cost a redesign,' 'answer or hand off, never bluff,' 'permissions proven, not promised,' 'benchmark before ship, then ship the boring default,' 'a system you have not tried to break is a system you know nothing about,' 'you rerun that sentence,' 'the red ones count too.' That is not engineer-explaining-to-a-peer; that is a keynote. The antimetabole cadence ('X is not X unless Y') is the LinkedIn-guru tic your voice rules were written to kill, wearing a lab coat. L-02 even opens on one ('not bugs in the permission system... bugs in where the permission system runs') and L-04 runs the humble-brag inversion ('The part I am least proud of is the strongest evidence for the approach'). Two maxims per package is a voice. Fourteen is a fog machine.

*Evidence cited:* Read every social file plus site/index.html and GUIDELINES.md. Maxim locations: L-01, L-02 opener, L-04 (linkedin/posts.md); thread tweets 4 and 7; X-06, X-07 closers (x/posts.md); IG-01, IG-06, IG-07 captions; T-02/T-03 close cards (tiktok/scripts.md); site pricing and footer sections (site/index.html). GUIDELINES.md §4 bans 'stacked one-line paragraphs for drama' and hype vocabulary but has no defense against epigram density.
*Suggested fix:* Ration: at most one recurring maxim per channel ('an alarm that always fires is no alarm' is the keeper, it is genuinely earned). Rewrite the rest as plain statements of fact with a path. Add an aphorism-density check to the voice review, since the linter cannot hear rhythm.

**Disposition:** **Fixed (rationed), with a judgment call logged.** Six of the fourteen maxims cut or flattened (T-02/T-03 closers, X-07 closer, T-01 close swapped to the already-earned 'publication is not detection', IG-01 'record you can edit' line, IG-06 'cannot make you look' caption). Kept: 'an alarm that always fires is no alarm' (the keeper, per the critic), 'publication is not detection,' and the X-06 KPI line, each doing real explanatory work at one use per channel. Full de-aphorization was judged to trade voice for blandness; the ration rule is now in the calendar review step.

### BR-3 (major)

> The voice rules are broken by the launch copy itself, in ways your own linter cannot see. GUIDELINES §4 bans 'rhetorical-question openers' outright, yet the launch thread's tweet 4 opens 'Why did nobody notice for seven weeks?' and X-04 opens 'How do you eval tenant isolation without trusting your own labels twice?' - two hard-ban violations in the two most-trafficked assets. The same tweet 4 is three stacked one-line paragraphs, each a freestanding aphorism, which is the precise 'stacked one-line paragraphs for drama' cadence the ban list names. The site footer adds a third question ('Found one?'). And your own X format rule says posts 'stay under 280 characters where possible' - I counted: eight of the ten standalone posts exceed 280 (X-09 is 348, X-01 is 321), which on X means either premium long-posts that fold your receipt behind 'Show more' or a forced rewrite on posting day. A brand whose entire pitch is 'we enforce our claims mechanically' is enforcing its voice claims with a grep that cannot see questions, cadence, or character counts - and it shows.

*Evidence cited:* Ran launch/tools/voice-lint.sh over all deliverables (all pass; the linter greps words only). launch/social/x/launch-thread.md tweet 4 (question opener + 3 stacked one-liners); launch/social/x/posts.md X-04 opener; site/index.html footer 'Found one?'; brand/GUIDELINES.md §4 ban list; launch/social/x/voice-and-format.md 280 rule; character counts measured by script: X-01 321, X-02 284, X-03 284, X-04 309, X-05 295, X-06 284, X-07 292, X-08 277, X-09 348, X-10 217.
*Suggested fix:* Rewrite tweet 4 and X-04 with declarative openers ('Nobody noticed for seven weeks. Two reasons, both mine.'). Trim the eight oversized X posts under 280 or explicitly amend the format rule to long-post format with the receipt above the fold. Extend voice-lint.sh with a question-opener check and a per-post character count.

**Disposition:** **Fixed.** Thread tweet 4 and X-04 rewritten with declarative openers; tweet 4 is a paragraph, not stacked one-liners; the site footer question removed. The X length rule is amended honestly (long-post format allowed, receipt must land in the first 280 characters) and X-01/X-03/X-04/X-09 were tightened. A cadence/char-count extension to the grep linter is logged as a future improvement; the voice review now includes a manual question-opener check (noted in x/voice-and-format.md).

### BR-4 (major)

> Your one semantic rule for color - 'pass green is the only accent and it means something (a passing check, a granted chunk); never use it decoratively' - does not survive contact with your own homepage. The primary CTA button ('git clone the receipts') is filled with --pv-pass green: a button is not a passing check, it is the most decorative pixel on the page. The timeline's left border is pass green. And the flagship chart card IG-06 draws seven weeks of documented failure - the 0.110 dead-leg plateau - as a continuous pass-green line, while IG-01 renders the identical numbers in fail red. Same data, opposite semantics, two cards apart, in a system whose entire visual thesis is 'green means verified.' If green means something, it has to mean it on the hero button and on the worst chart you own, or it means nothing, and you are back to being one more paper-white, mono-numeral, green-check dev-tool site, which in 2026 is the default eval-tool skin, not a brand.

*Evidence cited:* brand/GUIDELINES.md §7 rule quoted verbatim. site/index.html line 54: .btn{background:var(--pv-pass)} and line 63: .arc{border-left:3px solid var(--pv-pass)}. Viewed launch/social/instagram/cards/ig-06.png (entire series including 0.110 plateau in pass green) against cards/ig-01.png (0.110→0.140 in fail red).
*Suggested fix:* Recolor the CTA and timeline to ink/link neutrals (buttons are chrome, not verdicts). On IG-06, render the dead-leg segment in fail red or neutral chunk gray and let the line turn pass green only after 07-10; the fix date becomes the color transition, which is a better chart anyway.

**Disposition:** **Fixed.** The CTA button is now ink (buttons are chrome, not verdicts), the timeline border is neutral gray, and the IG-06 chart renders the failing period in fail red with the line turning pass green only after the 07-10 fix (card re-rendered; alt text updated). GUIDELINES §7 now encodes both rules so the next asset cannot repeat this.

### BR-5 (major)

> The name and the thesis are running different campaigns. 'Purvia' is coined from purview - it encodes the permissions feature - while the brand you actually chose is 'The Receipts Are the Product' with the tagline 'shows its work.' The name sells message 3; the tagline sells message 1; nothing marries them. Worse, the coinage loses its own morpheme: purview is PUR-vyoo, your guide says PUR-vee-ah, so nobody hears 'purview' and what they do hear is pharma-sweetener ('-via' as in Januvia, Truvia - and your own evidence found an actual withdrawn PURVIA sweetener trademark, plus PURVIA WATER). And the skeptical engineer you court, whose first act is verification, will google the name and land on a same-name consumer AI face-swap app - your own ledger's 'highest-risk collision,' unresolved - on a .com you do not own, parked by a squatter since 2012. A verify-everything brand whose name fails its own first verification is an irony this package cannot afford, and unlike the copy problems it cannot be patched after launch.

*Evidence cited:* brand/GUIDELINES.md §1 (coinage, PUR-vee-ah pronunciation, disclosed collisions); evidence/claims.md I2 (purvia.com parked since 2012, typo/email leak acknowledged), I3 (face-swap app 'IS software and presumptively the same Nice classes', 2008 sweetener application, PURVIA WATER), I4 (SERP 'except for queries the face-swap app already occupies'); THESIS.md launch precondition 3 (USPTO clearance pending); tagline vs name tension across GUIDELINES §1/§2.
*Suggested fix:* Before any spend: finish the Class 9+42 clearance (already your precondition) AND run a 10-person cold SERP/pronunciation test with ICP engineers; if the face-swap app owns the results or nobody hears 'purview,' rename now while the cost is one ADR. If the name stays, bridge name to thesis in the boilerplate ('Purvia, from purview: the receipts prove each viewer's purview held').

**Disposition:** **Partially fixed + accepted-as-precondition.** The name-to-thesis bridge line ('Purvia, from purview: the receipts prove each viewer's purview held') is now in GUIDELINES §2 for use wherever the name meets the tagline. The collision and pronunciation risks stand as stated and are THESIS precondition 4, now including the critic's 10-person cold SERP/pronunciation test. A rename was weighed and rejected while clearance is pending: the evidence (I4) still shows every alternative worse, and renaming twice in one week on an unlaunched brand costs more than it buys.

### BR-6 (major)

> The redemption arc's moral is contradicted by a live counterexample shipped in the same package. The story you sell: the dead leg taught the lesson, so now there is 'a lexical golden-set category so this leg is observable forever, and a one-sided alarm that cannot cry wolf.' The story you also disclose: the permissions-scale benchmark has published 0.000 in every cell since June 19, its 'regression alarm that is supposed to catch exactly this has been firing into a void,' and you found it while writing this marketing. That is the identical failure class - published red, alarm unheard, nobody looking - recurring AFTER the lesson was supposedly encoded, and still unfixed at launch. So the honest conclusion is not 'the instrumentation now protects me' but 'instrumentation without an operational reader is theater,' which L-04 gets right ('the other half is somebody actually reading the log') while the thread, IG-01, and IG-07 keep selling 'cannot recur unnoticed.' Pick one story. If the second dashboard also fired into a void, 'observable forever' is a promise the package has already broken once, mid-package.

*Evidence cited:* GUIDELINES.md §3 message 2 and site timeline ('observable forever', 'cannot cry wolf'); launch/social/x/posts.md X-09 ('The regression alarm fired into a void for weeks. I found it while writing marketing copy'); site/index.html not-yet section (same admission); THESIS.md precondition 1 (scale nightly red since 2026-06-19); docs/permissions-scale-nightly/ per claims J2; linkedin/posts.md L-04 (the only asset that draws the correct conclusion).
*Suggested fix:* Align the moral across all assets to L-04's version: the harness makes failure undeniable, it does not make you look, and the scale benchmark proves the lesson is not yet operational. Replace 'observable forever' with 'observable; whether I look is the discipline the scale benchmark shows I am still building.' Better: fix the scale benchmark before launch (it is already your precondition 1) so the arc ends fixed.

**Disposition:** **Fixed.** The moral is aligned to L-04's version everywhere: 'observable forever' is gone; the site arc, GUIDELINES message 2, thread tweet 7, and THESIS all now say the harness makes failure undeniable while the discipline of reading it is still being built, with the scale benchmark named as the standing counterexample.

### BR-7 (major)

> Your proudest receipt has a soft spot a skeptic will find in one read. The thread's tweet 8 crowns 'the thing that never broke: the zero-leak table... 1.000' through the whole dead-leg period, and the site repeats it ('only quality was broken'). But for those seven weeks the keyword leg was retrieving at 0.110-0.140 recall - a leg that returns almost nothing for authorized viewers exerts almost no pressure on the ACL for unauthorized ones. The keyword row of your 1.000 security table spent seven weeks largely proving that a dead leg cannot leak. The vector and hybrid rows carry the claim fine, but 'never broke, every mode, throughout' invites exactly the reply your X audience exists to write: 'your security eval passed because your retrieval barely worked.' For a brand that treats a structurally-vacuous pass as a hard failure elsewhere (B7's structural-blindness guard, E6's positive control - genuinely good design), not noticing the partial vacuity of your own flagship table is the kind of epistemic miss the whole package claims you do not make.

*Evidence cited:* launch/social/x/launch-thread.md tweet 8; site/index.html 'Throughout' timeline row; docs/nightly/2026-07-09.md (keyword recall@5 0.140 overall, single_chunk 0.250) read directly - the leg returned some gold, so the cell is weakened, not fully vacuous; evidence/claims.md B7/B9 show the team knows vacuous-pass risk and guards it in E6, making the omission conspicuous; A8 states the claim without this caveat.
*Suggested fix:* Add the one honest clause everywhere A8 is used: 'the vector and hybrid rows carried real attack pressure throughout; the keyword row proves less during the dead period, since a leg retrieving at 0.14 barely stresses the ACL.' It costs one sentence and buys the exact credibility the brand is priced in.

**Disposition:** **Fixed.** The keyword-row vacuity clause now travels with every use of A8: claims A8 (caveat added), thread tweet 8, the site's Throughout row. Exactly the one-sentence cost the critic priced.

### BR-8 (major)

> The datasheet is the best artifact in this package - the one genuinely differentiated move, because it fuses the founder's hardware-validation biography with the receipts thesis instead of asserting it - and it undercuts itself twice at the exact points a datasheet reader trusts most. First, its Sources footer cites 'launch/evidence/claims.md (full citation ledger)' - a path that is not in the public repo (launch/ is untracked; git ls-files returns zero entries), in a document whose stated premise is that every value 'carries the committed artifact it came from.' A dead citation in the citation ledger line is a claim without a receipt, in the artifact about receipts. Second, the Ordering Information table - the one section of a real datasheet that is contractually load-bearing - lists PURVIA-1P at $149-$249 for a part the same row admits is 'a hypothesis with an empty backlog.' Hardware people do not put vapor in the ordering table; that is where the parody stops being discipline and becomes decoration. Fix those two rows and this is the asset I would lead the launch with.

*Evidence cited:* extras/datasheet.html line 121 (Sources footer citing launch/evidence/claims.md); ran 'git ls-files | grep -c ^launch/' in /Users/hcho/Developer/purvia → 0 (untracked, not public); datasheet header 'A datasheet is a promise with test conditions... the committed artifact it came from'; Ordering Information table PURVIA-1P row (viewed in extras/datasheet-screenshot.png); THESIS.md: 'Nothing in this launch package presells any of it.'
*Suggested fix:* Either commit the claims ledger to the public repo (it is strong enough to be a receipt itself) or drop that citation from the footer. Move PURVIA-1P out of Ordering Information into a 'Future parts (unreleased)' note with no price, or publish the datasheet with a core-only ordering table.

**Disposition:** **Fixed.** Datasheet footer no longer cites an uncommitted path as a committed source (sources listed are all public-repo artifacts; the ledger is named as shipping with the launch package); PURVIA-1P moved to 'future part, unreleased, unpriced' with the vapor-pricing point conceded in the row itself. Whether to commit the ledger publicly is the owner's call (THESIS precondition 7 note).

### BR-9 (minor)

> The claims-ledger discipline leaks at the edges. '180 no-access runs a night' headlines IG-04, opens T-02, and anchors the site's closing line ('I have proven it leaks zero labeled chunks across 180 no-access runs a night') - but 180 appears nowhere in the ledger; it is derived arithmetic (60 questions x 3 modes) from a runner help-string, and GUIDELINES §5 says 'do not write new factual claims without adding them to that ledger first.' Worse, 'Every night' and 'a night' are the exact phrasing anti-claim J7 exists to kill: the series has an 18-day hole in June, which is why your house rule says '38 published snapshots, never every night.' IG-01's caption also says keyword recall 'sat at 0.110' when your own house phrasing insists 'drifted 0.110 to 0.140' - the card's table gets it right while its caption gets it wrong. Small, yes. But this package's one absolute rule is that copy never outruns the ledger, so every place it does is disproportionately expensive.

*Evidence cited:* launch/social/instagram/posts.md IG-01 ('sat at 0.110') and IG-04 ('Every night, 180 retrieval runs'); tiktok/scripts.md T-02 ('Every night, my eval runs'); site/index.html closing 'Who built this' line; grep for '180' in evidence/claims.md returns nothing (B6 carries no run count); evals/retrieval/runner.py --viewers help text (60 x 3 viewers) is the actual source; claims.md J7 and GUIDELINES house phrasing; docs/nightly/ listing confirms the 06-01..06-18 gap.
*Suggested fix:* Add the 180-run derivation to the ledger as its own ID (inferred, with the arithmetic shown); change 'every night' / 'a night' to 'per nightly run'; fix IG-01's caption to the house 'drifted 0.110 to 0.140' phrasing.

**Disposition:** **Fixed.** The 180 derivation is ledgered (A14, inferred, arithmetic shown); 'every night / a night' replaced with 'per nightly run' in IG-04 (card + caption), T-02, and the site; IG-01's caption now uses the corrected house phrasing (which itself changed per SE-7).

### BR-10 (major)

> The X banner - the single most-viewed pixel real estate the brand will own - reads 'zero-leak table 1.000 every cell - 38 nightly snapshots - false-resolve ceiling 5% - MIT.' Anti-claim J15 bans, verbatim, 'any compression of B6 that drops the gold-label scope,' and the house rule requires the scope clause 'whenever the 1.000 table appears.' This is precisely that compression, in the precise place a skeptic screenshots. You built an entire anti-claims section so that the one number most likely to be quoted against you always travels with its caveat, then stripped the caveat off the asset that travels furthest.

*Evidence cited:* Viewed extras/profile-assets/x-banner-1500x500.png (strapline verbatim); evidence/claims.md J15 ('or any compression of B6 that drops the gold-label scope'); GUIDELINES.md §4 house phrasing ('whenever the 1.000 table appears'). The LinkedIn banner avoids the number and is clean; checked banners.html for em dashes - the rendered long dashes are hyphens in the mono stack, so no dash violation.
*Suggested fix:* Two extra words fix it: 'zero-leak table 1.000 every cell (labeled gold)' - or swap the banner stat for one that carries no mandatory caveat, like '38 nightly snapshots, failures included.'

**Disposition:** **Fixed.** The X banner now reads 'zero-leak table 1.000 every cell (labeled gold)' - re-rendered and viewed. Two words, as priced.

### BR-11 (minor)

> The mark's story dies below 24 pixels. The whole semantic pitch - green chunks inside a closed predicate boundary, gray chunks outside - depends on the gray chunks being visible, and in your own preview screenshot the 16px favicon reads as a green rounded square with dots: the out-of-purview chunks, the entire reason the mark means anything, vanish. What survives is a generic app-grid glyph in CI-green, which is dev-tool default, not a brand. Meanwhile the cards' receipt typography - the actual signature - wraps file paths mid-word: ig-01 breaks 'docs/nightly/2026-07-11.md' before 'md', ig-03 breaks 'workspace_membership.sql' mid-token. In a system where the path IS the receipt, a path you cannot cleanly read is a smudged receipt. The receipt-panel system and honest red cells are genuinely distinguishable; the mark and these wraps are where the system slips back into the crowd.

*Evidence cited:* Viewed brand/marks/preview-screenshot.png (16px and 24px renders; gray chunks imperceptible at 16px); GUIDELINES §6 semantics ('gray chunks = exist in the corpus, outside the purview'); viewed cards/ig-01.png footer ('2026-07-11.' / 'md' across lines) and cards/_contact-sheet.png ig-03 footer ('workspace_m' / 'embership.sql').
*Suggested fix:* Cut a dedicated sub-24px favicon variant: boundary plus one gray chunk at higher contrast, or boundary-only. Add non-breaking treatment for paths on cards (break only at '/' or drop to a smaller full-width line).

**Disposition:** **Fixed (favicon) / fixed (paths).** favicon.svg is now a dedicated sub-24px variant (boundary + one granted block + one out-of-purview chunk at higher contrast). Card source paths now break only at slashes (<wbr> after each '/', word-break normal); all cards re-rendered and re-viewed.

## The career auditor

**Overall verdict (verbatim):** As a hiring screen this is a top-decile package - the claims ledger with its anti-claims section, the datasheet with a real errata table, and LinkedIn posts that actually survive being read aloud are better work samples than most staff candidates' portfolios, and every metric I diffed against the repo (38 snapshots, the 1.000 table across all of them, the 0.110-to-0.917 arc, the red scale benchmark) checked out exactly as written. But the package audits every number except the two a hiring panel checks first: the founder's own biography, which is the only unreceipted claim class on a page that dares readers to find one, and the 49 'Co-authored-by: Claude' trailers sitting on public main that the radical-honesty copy never mentions. Add an eight-retelling confession arc scheduled before the broken benchmark it apologizes for is fixed, and a pricing page for a tier that by its own admission contains nothing, and the panel's negligence-plus-flight-risk framing currently edges out the rigor framing. The fixes are cheap and mostly subtractive - ledger the bio, disclose and sell the agent workflow, give the confession an operational ending, cut the empty pro tier, and quarantine the launch/ folder - and with them the rigor framing wins comfortably; without them the package's own honesty standard is the rope it hangs by.

### CAR-1 (major)

> The one class of claim I will personally verify before anyone on my panel opens a nightly snapshot is the biography, and it is the only class of claim in this entire package with no receipt. 'I spent ten years in hardware validation' (L-01), 'At Zebra I validated hardware' (L-04), 'ten years in hardware validation and two in applied AI consulting' (site About), 'one engineer with ten years of hardware-validation background' (LinkedIn bio), 'Ten years in hardware validation and I still shipped this' (IG-07). Not one of these carries a claim ID; claims.md has sections A through J and no section for the founder. Meanwhile the footer of the same landing page says 'This page makes no claim without a path. Found one? Open an issue titled claim without a receipt.' You built a 130-row citation ledger and exempted yourself from it. I will diff 'ten years' against your resume and your LinkedIn employment history in the first five minutes of a screen, and if it is nine years or if 'validation' was actually two of those years, the entire receipts brand dies on the claim you chose not to ledger. This is major as written and a kill if the number is rounded.

*Evidence cited:* launch/social/linkedin/posts.md:12,58; launch/site/index.html:292 (About) and :301 (footer challenge); launch/extras/profile-assets/bios.md:14; launch/social/instagram/posts.md:60 (IG-07); grep of launch/evidence/claims.md shows sections A-J only, zero rows for biography/Zebra/years of experience; GUIDELINES.md:5 requires every factual claim in canonical copy to carry a ledger ID.
*Suggested fix:* Add a section K (founder biography) to claims.md with employer, exact dates, and titles, verified against the actual resume, statused 'verified'; then make L-01, L-04, the site About, and the LinkedIn bio match those rows to the month. If any number needs rounding, round down and say so.

**Disposition:** **Fixed (ledgered) + owner verification gate.** See SE-5: claims section K exists, the site says 'about ten years,' and precondition 5 blocks launch until the rows verify against the resume, rounding down if in doubt. The employer reference (K2) is explicitly flagged as an owner keep-or-cut decision before L-04 posts.

### CAR-2 (major)

> You confessed the dead leg, the red benchmark, the self-merging, and the empty pro tier, and you never once mention that 49 of the 152 human commits on your public main carry 'Co-authored-by: Claude Opus 4.8' trailers, with AGENTS.md, CLAUDE.md, and six agent-task PRDs committed in plain sight. Any hiring manager who runs git log, which is the first thing we do, sees the trailers on page one. The screenshot in my hiring channel writes itself: 'he disclosed everything except the co-author.' On a package whose entire brand is radical disclosure, the omission reads as the one thing you hoped nobody would notice, which converts your honesty asset into a liability. And it is a double loss, because what the trailers actually document, a solo engineer running PRD-driven agent development where the eval gates are the only reviewer that never sleeps, is the most current staff-level skill in this repo, and you are hiding it instead of selling it.

*Evidence cited:* Ran `git log --format='%b' | grep -ci co-authored-by: claude` in /Users/hcho/Developer/Purvia: 49 hits across 254 commits (152 by Hyung Cho, 102 by github-actions[bot]); .claude/agent/tasks/ contains 6 committed PRDs; AGENTS.md at repo root; zero mentions of AI-assisted development in THESIS.md, site/index.html, or any of the 26 social posts.
*Suggested fix:* Own it on the hiring platform: one LinkedIn post (or a paragraph in L-04) stating the workflow plainly - PRDs, story-scoped agent runs, seam-first delivery, and deterministic CI gates as the reviewer of record precisely because the commits are co-authored by a model. It is on-brand, verifiable in the same git log, and turns the discovery risk into the strongest differentiator in the package.

**Disposition:** **Fixed, and converted into content.** The disclosure is now proactive in the three places a hiring manager will look: L-04 gained the workflow paragraph (PRDs, seam-first delivery, eval gates as reviewer of record, 49 of 152 commits co-authored), the new L-05 is built around it, and the site About states it with the git log as the receipt. Claims K4 ledgers the counts.

### CAR-3 (major)

> The seven-weeks confession appears in the X thread (tweets 1, 4, 7), L-01, IG-01, IG-06, IG-07, T-01, and the landing page hero: eight retellings scheduled inside fourteen days. Told once, it is rigor. Told eight times, it is a personal brand built on a single incident, and it invites my panel to ask the second-order question the copy never answers: docs/nightly has zero files between 2026-05-31 and 2026-06-19, an eighteen-day outage of the 'nightly' series that every deliverable compresses into the parenthesis '(the series has gaps)', and the scale benchmark has been red for almost a month with the fix still pending at the time these posts are drafted. That is not one lapse honestly disclosed; that is a pattern - instrumentation excellent, operations habitually unattended - and the pattern, not the confession, is what a hiring committee will conclude. The negligence framing beats the rigor framing exactly as long as the story has no operational ending. L-04's 'the operational discipline lagged it' is the right sentence; the package schedules eight confessions before doing the one thing that would prove the discipline changed.

*Evidence cited:* Ran `ls docs/nightly/*.md`: files jump from 2026-05-31.md to 2026-06-19.md; docs/permissions-scale-nightly/2026-07-15.md still publishes 0.000 (red since 06-19, per THESIS launch precondition 1, still open); content-calendar.md schedules the confession on 07-20 (X + IG), 07-21 (L-01), 07-22 (T-01), 07-27 (IG-06), 07-30 (IG-08); no deliverable explains the June gap.
*Suggested fix:* Execute THESIS precondition 1 (fix or publicly annotate the scale benchmark) and add one sentence explaining the June 1-18 nightly outage before L-01 posts; then cut the retellings from eight to at most four (thread, L-01, one card, one video) and let the fixed-and-instrumented ending carry the arc.

**Disposition:** **Fixed.** The calendar now carries an explicit confession budget: the dead-leg story runs in exactly four assets in the fortnight (thread, L-01, IG-01, T-01); IG-06 and IG-07 moved to the week-3+ queue and their calendar slots went to capability content. The June outage now has its one-sentence explanation everywhere the arc appears (SE-2), and the operational ending the critic asked for is enforced by the preconditions gate rather than promised in copy.

### CAR-4 (major)

> The pricing section is the weakest thing on an otherwise strong page, and it costs you on both axes at once. The site sells 'Pro - $149 to $249, one-time (planned)' whose own bullet says it 'gates only what does not exist yet'; the datasheet prints an ordering table for PURVIA-1P, 'currently a hypothesis with an empty backlog'; THESIS admits the tier has no content and the kill criteria already rehearse retreating to the job search. To a buyer that is a price tag on an empty box. To a hiring panel it is worse: it signals commercial intent without commercial competence - founder-flavored enough to read as a six-month flight risk, empty enough to prove no customer has ever been talked to. You dilute the engineer signal and collect zero revenue signal in exchange. The honest version of this launch has no pricing page, because there is nothing priced.

*Evidence cited:* launch/site/index.html:270-288 (#pricing, 'planned', 'gates only what does not exist yet'); launch/extras/datasheet.html ordering-information section (PURVIA-1P 'a hypothesis with an empty backlog'); THESIS.md:63-73 ('the pro tier's content is a hypothesis with an empty backlog') and kill criteria 1-2 (fold back to portfolio/employment).
*Suggested fix:* Delete #pricing from the launch site and PURVIA-1P from the datasheet; keep the 'Core - free, MIT, forever / a receipt behind a paywall is not a receipt' framing as a licensing statement, and run the 20 willingness-to-pay conversations privately as THESIS already plans.

**Disposition:** **Fixed (reframed), with the brief's constraint logged.** Prices are off every launch surface: the site pro panel and datasheet part row are now 'planned, unpriced,' with the $149-$249 band demoted to the hypothesis under test in private buyer conversations. Full deletion was rejected because the mission brief mandates the open-core commercial shape; the evidence-based amendment (display no price, test the band privately) is logged as the third pricing challenge in THESIS.

### CAR-5 (major)

> The package systematically undersells the artifacts a hiring panel actually weighs. Six committed ADRs and only ADR-0009 is ever linked anywhere; six PRDs with per-story status; a domain glossary in CONTEXT.md; a named seam-before-call-site shipping pattern; 42 backend test modules against 27k lines of backend Python - your own verified claim D8 - and none of it appears in the four LinkedIn posts, which are the only deliverables aimed at the people who hire. All four posts sell the eval story; zero sell the software-engineering-process story, which for a staff role is the half we probe hardest. You wrote 'ship the seam before the call-site' into your project invariants and never once said it to the audience that would hire you for it.

*Evidence cited:* ls docs/adr/ shows 6 ADRs (0001, 0002, 0007, 0009, 0010, 0011); grep of launch/ finds ADR links only to 0009 (site) and 0011 (GUIDELINES); .claude/agent/tasks/ holds 6 PRDs; claims.md D8/D9 (verified, cited nowhere in social copy); linkedin/posts.md L-01..L-04 contain no ADR, PRD, or test-scale reference; linkedin/voice-and-format.md:3 itself says LinkedIn 'is where the builder gets evaluated.'
*Suggested fix:* Add a fifth LinkedIn post (or rework L-04's middle) on how the repo is run: ADR discipline with two of the six linked, PRD-to-story decomposition, the seam-first pattern with the US-115/116 example, and the 42-module test surface. Pair it with CAR-2's disclosure and it becomes the strongest post in the package.

**Disposition:** **Fixed.** New L-05 ('How a solo repo earns a second reviewer') sells the process evidence: ADR discipline, PRD decomposition, seam-before-call-site with the US-115/116 example, the 42-module/14k-line test surface, and the deterministic-gates-as-reviewer frame, ending on the honest limit. Paired with the CAR-2 disclosure as recommended; scheduled in the week-3+ queue so the launch fortnight's LinkedIn cadence holds.

### CAR-6 (major)

> The launch/ folder containing this entire war room sits untracked in the working tree of the public repo, one `git add -A && git push` away from shipping, and nothing in the package marks it never-commit. If it lands on main, linkedin/voice-and-format.md line 3 - 'LinkedIn is not where the kit gets adopted; it is where the builder gets evaluated' - plus the calendar's 'the postmortem frames the same event for hiring managers' becomes the screenshot that reframes every 'confession' post as engineered copy with claim IDs and a posting grid. Your brand survives the marketing being rigorous; it does not survive the marketing-about-the-marketing going public by accident in a repo whose whole pitch is that everything is public.

*Evidence cited:* git status in /Users/hcho/Developer/Purvia shows `?? launch/` (untracked, inside the public repo's working tree; origin = github.com/hcho22/Purvia, confirmed public via api.github.com 200); launch/social/linkedin/voice-and-format.md:3; launch/social/content-calendar.md:11; no .gitignore entry or warning file for launch/ found.
*Suggested fix:* Move launch/ out of the public repo's working tree entirely (or at minimum add it to .gitignore with a README-level warning). Decide deliberately which artifacts, if any, are meant to be public - the claims ledger arguably strengthens the brand; the platform-strategy files never will.

**Disposition:** **Fixed (gitignore) + owner decision.** See SE-9: `launch/` and `gtm/` are ignored, so the one-command accident is now impossible without deliberately removing the ignore rule. The deliberate-publication question (ledger and datasheet would strengthen the brand; platform-strategy files would not) is THESIS precondition 7's owner note.

### CAR-7 (minor)

> X-03 posts 'My favorite line of code I wrote this year is an error message.' I checked: the commit that introduced that line carries no model co-author trailer, so you survive the literal diff - but you are one quote-tweet away from someone running the same check across a log where 49 commits say 'Co-authored-by: Claude Opus 4.8', and singular authorship claims are precisely what that crowd stress-tests. On a package this disciplined about phrasing ('fails the required CI check', never 'blocks the merge'), 'I wrote' is the one loose formulation left on the table.

*Evidence cited:* launch/social/x/posts.md:21 (X-03); `git log -S "cannot be downgraded" -- evals/gate/declaration.py` resolves to fff8e52 (US-102, PR #61), whose body contains no co-author trailer; repo-wide count of Claude trailers: 49.
*Suggested fix:* Rephrase to 'My favorite line of code in my repo this year is an error message' - identical punch, immune to the authorship diff.

**Disposition:** **Fixed.** X-03 now reads 'My favorite line of code in my repo this year' - same punch, immune to the authorship diff.

### CAR-8 (minor)

> L-02 tells my feed the CI eval 'asserts zero retrieved rows across the boundary.' The eval asserts recall@10 == 0.0 against the second workspace's labeled gold chunks, with a positive control - which the landing page states precisely and the LinkedIn post compresses into a stronger claim. This is exactly the compression class your own anti-claim J15 polices, committed on the one platform where an interviewer will read the sentence aloud and then open e6.py. Small, but for a candidate whose pitch is that his sentences survive diffing against his repo, every sentence that does not is disproportionately expensive.

*Evidence cited:* launch/social/linkedin/posts.md:34 ('asserts zero retrieved rows across the boundary') vs evals/retrieval/e6.py:26-28 ('The negative assertion is recall@10 == 0.0 against B's gold for every (mode x filter) row'); site/index.html:180 uses the precise wording; claims.md J15 documents the gold-label scope rule.
*Suggested fix:* Match the site's wording in L-02: 'asserts recall@10 of 0.0 against the other workspace's labeled gold chunks, with a positive control so a vacuous pass fails too.'

**Disposition:** **Fixed.** L-02 now uses the precise E6 wording: recall@10 of 0.0 against the other workspace's labeled gold chunks, with the positive control named.
