# Profile bios (per platform, sized to each platform's limit)

Status: DRAFT ONLY - no accounts created, nothing published (guardrail §3.2).

**X (160 chars max):**
> Permissions-aware RAG kit. ACLs in the SQL predicate, zero-leak evals in CI, a support bot with a 5% false-resolve ceiling. MIT. Receipts in the repo.

(154 chars)

**LinkedIn company/creator headline:**
> Purvia - the RAG kit that shows its work. Permissions enforced in the retrieval predicate and proven by evals in CI; escalation safety measured against a buyer-set ceiling. MIT core.

**LinkedIn about (first 2 lines shown before the fold):**
> Purvia is an MIT-licensed, permissions-aware RAG kit built by one engineer with ten years of hardware-validation background.
> Chunk-level ACLs are enforced inside the retrieval SQL predicate under the viewer's JWT, and the claim is checked, not asserted: zero-leak evals and API attack suites fail their CI check on any breach, 38 nightly eval snapshots are committed to the repo including the failures, and the support bot's escalate-vs-answer decision is deterministic control flow held under a 5% false-resolve ceiling. The known weaknesses are documented with the same discipline as the features. Everything is verifiable at github.com/hcho22/Purvia.

**Instagram (150 chars max):**
> Receipts from a permissions-aware RAG kit. Real eval numbers, disclosed failures, MIT. Every card cites its file path.

(118 chars)

**TikTok (80 chars max):**
> RAG evals, live in a terminal. The failures stay in the repo.

(61 chars)

**Assets in this folder:** `avatar-800.png` (all platforms; crops safely to a circle), `x-banner-1500x500.png`, `li-banner-1584x396.png`, source `banners.html` (re-render by screenshotting each element).
