# Project agent memory

This file is the project's committed home for durable, project-intrinsic agent knowledge: the cross-cutting invariants that prevent mistakes.
It is deliberately lean (context engineering: carry the high-signal rules, not the history).
The full per-story detail, rationale, and `Test:` commands for the Epic-E support widget + Phase-2 eval-gate work (US-066 through US-093) live in `docs/support-widget-internals.md`, the merged PRs (#51-#70), and git history.

- Add durable project-specific notes here as they are discovered through real work; put per-story specifics in `docs/support-widget-internals.md`.

## Core invariants (do not violate)

These are distilled from the whole Epic-E surface. If a change appears to require breaking one, stop and reconsider - each is load-bearing for tenant isolation or the deflection metric.

1. **`role` and `is_bot` are administrative-only flags - NEVER in a retrieval/visibility predicate** (ADR-0002). `workspace_membership.role` and `is_bot` must not appear in `match_chunks` / `keyword_search` / chunks / conversations RLS, or any visibility gate. The bot is `role='member'` and sees only what `chunk_acl` shares to it. The ONE legitimate exception: `role='admin'` gates the `widget_keys` admin-management RLS (issue/rotate/revoke is the administrative action the role exists for).

2. **Four trust boundaries, kept apart** - do not merge them: (a) owner-only `threads`/`messages`; (b) workspace-membership `conversations`/`conversation_messages` (see next section); (c) the opaque per-conversation customer token (backend-mediated, deny-all RLS); (d) the non-secret widget public key (names a workspace's bot, grants nothing). Never collapse the two conversation-table models into one `kind`-discriminated table.

3. **Secrets are server-side only - never an SSE event, response body, or log line.** This covers the minted bot JWT (US-068), the `SUPABASE_SERVICE_ROLE_KEY`, and the raw customer token (US-071, returned exactly once in the `X-Conversation-Token` response header, never in an SSE event). The widget `public_key` is the only non-secret and may be embedded client-side.

4. **fail-CLOSED where a miss leaks; fail-SOFT where a miss is cosmetic.** Auth / escalation / grant paths fail closed (e.g. `_reject_if_bot_principal` raises rather than risk a silent bot share; a bad token → 401/404; a botless/degraded deflection turn → generic deferral). Presentation filters fail soft (e.g. `get_shares` bot-row filter falls back to an unfiltered list rather than 500 the read). Never invert these.

5. **One-way latches are DB-enforced, not just service-layer.** `conversations.status` moves only `active → escalated → resolved` (trigger-guarded; resolved is terminal). `widget_keys.revoked_at` cannot be cleared or moved (trigger). Resolving a conversation purges its customer tokens (AFTER trigger). Do not re-implement these as app-layer checks.

6. **`escalated_at` is trigger-owned - callers NEVER plant it.** The US-067 status trigger stamps it on the first transition into `escalated` and preserves it verbatim thereafter. This is what keeps deflection **derivable, not stored**: `resolved AND escalated_at IS NULL` ⇒ deflected (bot alone); `resolved AND escalated_at IS NOT NULL` ⇒ human-handled. Never add a `resolved_by_bot`/`resolved_by_human` column.

7. **`filter_workspace_id` is a non-security narrowing filter, never the boundary.** On `match_chunks`/`keyword_search` it can only subtract within what the `auth.uid()` membership clause already allows; `null` is a no-op. The trust boundary is always the membership EXISTS clause resolved from the principal's JWT, never this param.

8. **Escalate-vs-answer is deterministic control flow, never a model `escalate()` tool** (ADR-0003). The deflection pipeline decides; a tripped circuit breaker or a weak-retrieval/unfaithful draft/grounded non-answer latches escalation. **Grounding and answering are ORTHOGONAL and BOTH gate the send path** (issue #97): the faithfulness gate proves the draft doesn't lie, the answer-completeness gate (`escalation.answer_gate`, `ESCALATION_ANSWER_CUTOFF`) proves it actually ANSWERS - a grounded "I don't have that information" clears faithfulness (zero unsupported claims) yet must escalate, never auto-send as a silent deflection. Both gates fail CLOSED, and "fails closed" includes the SAMPLER: both runtime gates pin `JUDGE_TEMPERATURE` (default 0), which REMOVES THE SAMPLER as a source of variance in the send/escalate verdict for an identical (question, draft). That is best-effort, not a guarantee - no `seed` is passed and provider-side temperature 0 is itself best-effort, so a differing verdict is unlikely, not impossible; the measured defect the pin removes is real (`answers=true` on 2 of 5 identical calls, issue #104). A judge call is EXACTLY ONE call with no retry: a deployment that REFUSES THE ARGUMENT (OpenAI o-series / `gpt-5-*`, at any value including the default) is a configuration fact an operator states once with `JUDGE_TEMPERATURE=none`, never something inferred from a provider's free-text 400 (classifying those on a safety path is unsafe in both directions); an endpoint that instead refuses a VALUE as out of its own narrower range is a DIFFERENT case - it cannot fire at the `0` default and is fixed by choosing a value that endpoint accepts, not by un-pinning a gate that could have stayed pinned. A boot-time warning names KNOWN refusing model names only (`escalation._TEMPERATURE_REFUSING_MODEL_PREFIXES`, minus any name carrying the non-reasoning `_NON_REASONING_CHAT_MARKER` convention); it is best-effort and wrong in BOTH directions - an unrecognised model gets no warning, the deliberately broad `-chat` exclusion errs toward MORE missed warnings rather than more false alarms, and a name match is a family guess rather than an observed refusal, so the message stays conditional and tells the operator to verify before un-pinning. It never blocks startup or touches a gate, and it is SCOPED to the widget surface (`support_configured=`, supplied by the caller from `SUPABASE_SERVICE_ROLE_KEY`) - the gates run only on the support path, so per invariant 10 a knowledge-assistant-only deploy must never see it; a warning whose PREMISE does not hold for its reader is as wrong as one that overclaims. **The answer gate's test is whether the CUSTOMER ends up holding what they asked for, not whether the draft addressed the topic** (issue #104): when the corpus's own answer is a deferral - "quoted per-case", "at the discretion of customer service" - a faithful restatement of it is fluent, on-slot, and still leaves the customer with nothing, so it must escalate. Do not let the rubric collapse back into a disclaimer detector that only catches a draft ANNOUNCING its ignorance: whether the drafter volunteers "I don't have that information" is its own sampling artefact, not a safety signal. The runtime rubric and its offline E7 mirror (`evals/retrieval/runner.py::ANSWER_JUDGE_PROMPT_TEMPLATE`) state the same rules and must be changed in LOCKSTEP - drift makes the weekly false-resolve number a measurement of something the buyer does not ship (`backend/test_answer_gate_rubric.py` pins both, as rule PRESENCE in each rubric rather than full textual parity - `_SHARED_RULES` is a hand-curated list, so it catches a rule dropped from one side, not a rule added to only one). A degraded/transient failure defers this turn but does NOT latch (may recover); only a deliberate escalate latches the bot silent forever.

9. **Ship the seam before the call-site.** New cross-cutting mechanisms land as an abstract seam with its own test, then a later story wires the live call-site (US-075→076, US-077→079, US-082→081, US-084→081). Follow this pattern for new infrastructure.

10. **Support is optional and fails closed at the boundary.** With no `SUPABASE_SERVICE_ROLE_KEY`, every `/widget/*` route 503s (inert, never unprotected) and the rate limiter/breaker are a clean no-op. With no `SUPABASE_JWT_SECRET`, the bot degrades to the generic deferral rather than answering. A knowledge-assistant-only deploy leaves both unset and is unaffected.

11. **Every seeder inserts its own `workspace_membership` rows for every principal it creates.** The `20260617120200` backfill only captures users that existed at MIGRATION time; seed users are inserted *after* migrations run. The US-003 clause is AND-ed with the owner-OR-ACL predicate, so a principal with no membership row sees NOTHING - not chunks it owns, not chunks explicitly granted to it in `chunk_acl`. A seeder must stamp `DEFAULT_WORKSPACE_ID` explicitly on the documents it inserts AND join its principals to that same constant - never rely on the `workspace_id` column DEFAULT, which `20260617120200` itself labels TRANSITIONAL and US-007 may drop or repoint. `db_seed/corpus_seed.py` and `db_seed/wikipedia_seed.py` each have an idempotent `_ensure_workspace_membership`; a new seeder without one silently retrieves zero rows.

12. **"Measured nothing" must never be reportable as a measurement, and the path that should shout must never be the path that goes quiet.** An empty gold set means the run measured nothing, and "retrieved nothing" must never collapse into the same reportable number as "retrieved the wrong things" - a `return 0.0` publishes a well-formed table indistinguishable from a real regression (that is what hid invariant 11 for two months of nightlies). Guard where the degeneracy is first knowable, not only inside the scoring function; name the cell (principal / question / mode / viewer / knob) in the message; and never let a failed run leave the previous run's summary behind for the publish step to commit. This binds for DERIVED gold (`evals/permissions_scale/runner.py::DegenerateRunError`, pinned by `test_degenerate_guard.py`) **and** AUTHORED gold (`evals/retrieval/content_anchors.py::EmptyGoldError`, raised by the E4 scorers, `_metrics_block`, `resolve_gold_anchors`, and E6's `recall_at_10` / `b_gold_for`; pinned by `evals/retrieval/test_empty_gold_guard.py`). E6's was the sharpest instance: its negative assertion IS `recall@10 == 0.0`, so an empty B-gold set scored a **PASS** on a tenant-isolation invariant the run never exercised. The same rule governs CI. A runner that dies before writing its snapshot has produced no verdict, so the weekly workflows must report that as its own **UNMEASURED** state - a deduped `*-harness-failure` issue plus a failed job - and never as a gate finding; an `exit 0` on "no snapshot" is what kept the E7 and RAGAS weeklies from filing a single issue across months of red runs.

## Two trust models for conversation-shaped tables (do not merge)

Two parallel chat-message table pairs exist on purpose, with **different RLS boundaries**:

- `threads` / `messages` (`supabase/migrations/20260416120000_init_threads_messages.sql`) - the knowledge-assistant surface. **Owner-only**: `auth.uid() = user_id`, child delegates to its thread's owner. This is the leak-proof core the E4/E6 evals pin; it must stay untouched.
- `conversations` / `conversation_messages` (`supabase/migrations/20260623120000_init_conversations.sql`) - the support-widget surface (Epic E). **Workspace-membership**: the ADR-0002 EXISTS-against-`workspace_membership` clause (`wm.workspace_id = … and wm.user_id = auth.uid()`), child delegates to the parent conversation's workspace. Any member of the workspace can read/claim the queue.

Do NOT collapse these into one table with a `kind` discriminator branching the policy - that puts two trust models in one predicate (PRD Risk #3 / ADR-0004 reject it). Cross-workspace zero-leak is pinned by `backend/test_us066_conversations_rls.py`. Per-table specifics are in `docs/support-widget-internals.md`.

## How to test

- Complete offline backend suite: follow [README § Tests and evals](README.md#tests-and-evals). Keep that contributor contract and `backend/run_offline_tests.py::INTEGRATION_ONLY` authoritative when adding backend tests.
- Backend story tests are `python -m backend.test_usXXX_*` (plus `test_conversation_status_machine`, `test_supabase_jwt`, `test_au4_auth_attacks`). Each has a **unit layer that always runs** (no DB/secrets) and an **integration layer that skips cleanly** without a local Supabase. The full module list is in `docs/support-widget-internals.md`.
- Cross-workspace zero-leak: `python -m backend.test_us066_conversations_rls` (needs local Supabase + `DATABASE_URL`). API-edge auth attacks: `python -m backend.test_au4_auth_attacks`. DB-layer RLS-hardening regression (the Purvia RLS-audit F1/F2/F3 tightenings): `python -m backend.test_sec_rls_hardening` (needs local Supabase + `DATABASE_URL`; clean-skips without it). The last two are merge-blocking per-PR gates: they ride the `au4-security-invariants` job together (AU4 first, RLS-hardening second on the same booted stack).
- Eval-harness guard tests run offline with no DB or secrets: `python -m evals.permissions_scale.test_degenerate_guard`, `python -m evals.retrieval.test_empty_gold_guard`, and `python -m evals.retrieval.test_us120_generation_guard` (the three halves of invariant 12 - derived gold, authored gold, and the gpt-5.6-luna generator's truncated/empty-answer guard; all gated per-PR by the `eval-harness-guards` job in `.github/workflows/retrieval-eval.yml`). That job now runs **eight** offline modules: those three plus `evals.retrieval.test_judge_sdk_compat` (the offline Claude judge's `anthropic` pin must stay below the 1.0 major that removed the `temperature` kwarg - the drift that crashed both weeklies at the deterministic judge, issues #124/#125), `evals.retrieval.test_e7_answer_gate_parity` (exact-input offline/runtime replay; runtime failure is UNMEASURED, never false agreement), `backend.test_answer_gate_rubric`, `backend.test_answer_gate` and `backend.test_faithfulness_gate` - the only per-PR enforcement of the invariant-8 rubric lockstep, parity harness, and `JUDGE_TEMPERATURE` pin. A change to any of the eight, to `backend/escalation.py`, or to `backend/main.py` re-runs it, because the edit that must never escape a guard is the edit that weakens it.
- `python -m backend.test_answer_gate_rubric` is the one test whose integration layer skips on missing **API keys** rather than a missing Supabase: it makes LIVE judge calls, because the thing it pins is what the invariant-8 rubric CONCLUDES, and a recorded fixture would only replay a canned verdict (that is what `test_answer_gate.py`'s fake judge already does). Its unit layer needs nothing and pins the runtime/offline rubric lockstep. If you edit either rubric, run this with `OPENAI_API_KEY` + `ANTHROPIC_API_KEY` set - the offline layer alone cannot tell you the change still discriminates.
- Frontend gate is `npm run typecheck && npm run build` (multi-page; `tsc` is the lint gate - no ESLint config).
- Manual browser QA for the deferred UI checks: `docs/manual-test-plan-support-widget.md`.

## Where the full detail lives

- `docs/support-widget-internals.md` - the per-story sharp edges (US-066-093), rationale, and test descriptions relocated from here.
- `docs/widget-embed.md` - the loader `<script>` / cross-origin iframe embed contract.
- `docs/evals.md` + `docs/golden-set-authoring.md` - the eval harness, gate classes, and golden-set authoring.
- `docs/adr/` - committed ADRs (0001 RAGAS, 0002 tenant isolation, 0007 ingestion boundary, 0009 keyword OR-fallback, 0010 deterministic-alpha fusion, 0011 product name Purvia, 0012 Luna answerer / judges unchanged, 0013 gpt-5-mini runtime judge + `reasoning_effort` knob). ADR-0003/0004/0008 are cited but not committed; their record is the internals doc + PRs #51-70.
- `CONTEXT.md` - domain language; `.claude/agent/tasks/prd-phase2-implementation.md` - the Phase-2 PRD + per-story status.

## Maintaining this file

Keep this file for knowledge useful to almost every future agent session in this project.
Do not repeat what the codebase already shows; point to the authoritative file or command instead.
Prefer rewriting or pruning existing entries over appending new ones.
When updating this file, preserve this bar for all agents and keep entries concise.
